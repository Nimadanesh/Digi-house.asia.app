# FRACTIONALLUXE PROGRAM — working file for THIS repo (the Telegram Mini App)

> You are an AI coding agent working in the **app.fractionalluxe** repo (the FractionalLuxe Telegram Mini App on TON). This file is your single source of truth for program work in this repo. It is maintained by the founder's planning session (master plan lives in the site repo, `FL/docs/product-roadmap/`) — do not restructure it; you may only tick checkboxes (`- [ ]` → `- [x]`), update the Resume Here block, and append to the Progress Log.
> **Created:** 2026-08-23 · **Model target:** fast executor models — follow steps literally, verify each "done when", don't improvise.

---

## 0. Resume Here (update after every session)

| Field | Value |
|---|---|
| Current phase | **Phase 9 active — Estate Page Structure refactor, villa-by-villa: VILLA 1 + structure + revision contract + wave 3 (Yield removed / Earn tab) + wave 4 breathing pass (typography/spacing/Earn beam permanent) applied 2026-09-14 (uncommitted, this session); villas 2–24 next (one per step, user-gated)** |
| Next step | **User review of the breathing pass (report delivered; STOP — do not continue automatically) → on approval, villa 2 of 24 per structure §10**; prior pending: user review of PROMPT 05 → Prompt 06, UI polish #07a/#07b user visual verify, I0 (contract verify), branch divergence reconcile (local ahead / remote behind), pre-existing eslint.config.mjs + PropertyGallery.tsx disposition |
| Blockers | No Docker on this machine → `infra:up`/`db:migrate`/`db:seed` runtime verify pending (A3 dry-run + tests cover the code path); U1/U3/U4/U6 still open |

## 1. Context you need

- **Product:** FractionalLuxe — fractional ownership of luxury villas (Dubai, Bali, Phuket, Mykonos, Marbella, Maldives). Investors buy shares from **$80** in this Telegram Mini App; the marketing site (`fractionalluxe.com`, separate repo) sends users here via `t.me/<bot>/app?startapp=prop_<propertyId>` deep links.
- **Locked income model (user-confirmed 2026-08-23):** rental income **accrues monthly**. Withdrawals are on request: **1% fee, paid in 4 weekly installments**. All copy must match.
- **Locked brand:** name FractionalLuxe; palette = Telegram azure `#229ED9` + slate ink `#17212B`; logo files provided in step A2. No other visual direction.
- **The propertyId contract:** the 24 property IDs in the manifest (step A3) are shared with the site. Never rename, never invent new IDs.

## 2. DO / DON'T

**DO**
- Read this repo's own `AGENTS.md` / `README.md` / `apps/api/README.md` first and follow its conventions (npm workspaces, Next.js, Postgres+Redis).
- One step at a time: execute → verify "done when" → tick → continue. Minimal diffs; never reformat lines a step doesn't name.
- Run the repo's checks (`npm run check`, tests) at every phase gate and record the result.
- Log every mapping decision you make in A3 — the user reviews them.
- If a verification fails 3 times: `git checkout -- <files>`, mark the step BLOCKED in the log, move to the next unblocked step.
- Append one dated Progress Log entry per session (section 6).

**DON'T**
- **No new dependencies** without explicit user approval.
- **No secrets/tokens in code or docs** (BotFather tokens, DB URLs beyond local dev examples).
- **Never modify payment/settlement/TON logic** beyond what a step explicitly names. If code semantics conflict with the locked income model, document it — don't "fix" it.
- **No fabricated data**: no fake trades that look real; keep the app's existing "simulated" disclosures.
- Don't touch the marketing site repo (you don't have it; the user coordinates).
- Don't `git commit`/`git push` unless the user asked in that session.

## 3. [NEEDS USER] queue (build around, never wait, never invent)

| ID | Item | Affects |
|---|---|---|
| U1 | Production bot username (deep links) | Phase I app steps |
| U3 | Telegram API creds (waitlist notifications) | I6 |
| U4 | KYC approach decision | A7 (documentation only) |
| U6 | Launch mode (public vs invite-only) | Phase I wording |

## 4. Phase A — App Improvements (no user inputs needed)

- [x] A0 **Setup & baseline.** Fresh clone of this repo; `npm install`; read `AGENTS.md`, `README.md`, `apps/api/README.md`. Start infra if Docker is available (`npm run infra:up` → `db:migrate` → `db:seed`); otherwise note it and skip DB-dependent runtime checks. Record `npm run check` result as the baseline (pre-existing failures are acceptable — list them).
- [x] A1 **Rebrand strings.** "DigiHouse" → "FractionalLuxe" in user-facing UI: `messages/*.json` brand key(s) in all 12 languages (translate the brand consistently — "FractionalLuxe" stays Latin in all locales), `src/app/layout.tsx` metadata, onboarding carousel copy, README title line. Keep env var names, package names, and repo URLs as-is. Done when `grep -rni "digihouse" src/ messages/ --include="*.ts*" --include="*.json"` returns only technical identifiers (env/db/package names), listed in the log.
- [x] A2 **Brand assets.** Import the two logo SVGs the user will place at the repo root (`fl-mark.svg`, `fl-logo.svg` — if absent, stop and log [NEEDS USER]) into `public/` per repo conventions; replace the app's logo/wordmark usages (header, onboarding, loading brand). Audit the app's primary color tokens and align to azure `#229ED9` / ink `#17212B` if trivially different; if the app uses a theming system that makes this non-trivial, log findings and stop that sub-step.
- [x] A3 **Portfolio migration to the 24-property manifest.** *(runtime DB verify BLOCKED: no Docker on this machine — dry-run `npm run db:seed:dryrun -w @digihouse/api` passes with 24/24, statuses funding/funded/resale; unit tests cover the id/price/shares contract)* The user will place `portfolio-manifest.json` at the repo root (exported from the site; if absent, stop and log [NEEDS USER]). Extend/replace the DB seed so exactly these 24 properties exist with identical `propertyId`, `pricePerShare`, `totalShares`; map remaining fields onto the app's property schema (defaults for app-only fields are fine; remove or 1:1-map legacy seed listings — log every decision). Idempotent: seeding twice changes nothing. Verify at runtime (marketplace/API shows 24, correct prices) if DB available; else land code + dry-run script and mark runtime verify BLOCKED. Keep the app's simulated-tape disclosures intact.
- [x] A4 **Income-model copy alignment (COPY ONLY).** Locked model: **monthly accrual; withdrawal on request → 1% fee, 4 weekly installments.** Current app copy says "weekly yield" (`WeeklyYieldCallout`, `messages/*.json`) — change accrual framing to monthly; "weekly" may remain ONLY in withdrawal/installment contexts. Add the withdrawal-terms line to earnings/withdrawal screens ("Withdraw anytime — 1% fee, paid in 4 weekly installments"). Update `messages/en.json` first, then mirror the same keys in all 12 languages (translate meaning; keep number tokens exact; if unsure, keep English and mark `// TODO translate` in the log). If payout/settlement code semantics conflict with the model, document — don't change code. Done when `grep -ri "weekly" messages/en.json` matches only withdrawal contexts.
- [x] A5 **Public read API.** In `apps/api`, add unauthenticated GET endpoints following existing route/middleware patterns (update the route table in `apps/api/README.md`): `GET /public/properties` → `[{ propertyId, title, destination, area, pricePerShare, sharesSold, totalShares, projectedNetYield }]`; `GET /public/properties/:id` → same + `{ recentTrades: [last 5 {price, qty, at}], fundedPct }`. No personal data, no orderbook depth, no writes. Reuse an existing rate-limiter if present; otherwise a simple in-memory token bucket. CORS: `https://fractionalluxe.com` + `http://localhost:3000`, config-driven. Add smoke tests following existing patterns.
- [x] A6 **Waitlist endpoint.** `POST /public/waitlist` `{ email, telegram?, propertyId?, utm? }` → validate email shape, persist (new `waitlist` table via migration, or the existing launch module if it fits), idempotent on email, `{ ok: true }`. No notifications yet (U3). CORS as A5. Test per repo patterns.
- [x] A7 **KYC gap — document only.** Confirm onboarding has no identity verification. Add 3–5 lines to README "Open items": "KYC pending decision U4; auth is Telegram initData only." Nothing else.
- [ ] A8 **Phase gate.** `npm run check` + tests green (baseline exceptions documented). Log A3 mapping decisions summary. Update Resume Here.
  - **2026-08-24 result:** `npm run check` ✅ (0 errors; 3 pre-existing lint warnings in `apps/api/src/e2e/money-path-*.test.ts`), root vitest 361/361 ✅, API vitest 505/505 ✅, `typecheck:api` ✅.
  - **A3 mapping decisions:** id/title/gallery 1:1 · pricePerShare→sharePriceUsd ×100 (cents) · valuationUsd→totalValueUsd ×100 · location=`"{area}, {destination}"` · status deterministic i%3 (funding/funded/resale) · sharesSold deterministic per status (40%/100%/60%) · annualRentUsd=round(valuation×yield%)×100 cents · description generated · meta defaults (sizeSqm 0, yearBuilt 2020, rented) with manifest projections carried verbatim in optional meta fields · monthlyYieldRate via existing yearBuilt rule ("7.26", within 4.5–7.5 check) · rentalHistory empty (no fabricated payments) · one dev owner-wallet placeholder · legacy `prop-*` seed rows deleted at seed time (warn-and-continue if FK-blocked). Seed source swapped to manifest only in production seed (`seed-properties.ts`); `properties-data.ts` fixtures untouched (used by 20+ test files).
  - **A4 note:** payout/settlement code still pays weekly (Sunday schedule) — semantics conflict documented, NOT changed, per DON'T rules. Display figures labeled "/ month" convert the weekly mock figure ×52/12 at the presentation layer.

## 5. Phase I & T — app-side steps (after user inputs / after Phase A)

- [ ] I0 **Contract verify.** Manifest IDs in DB == site manifest (24, prices match). Re-run after any seed change.
- [ ] I4 **startapp deep-link routing.** Parse `initData.start_param` (`prop_<propertyId>`, optionally `~utm_<source>`) → route straight to that property; unknown/absent → normal home. Follow app navigation conventions; add a unit test with a fake initData payload.
- [ ] I6 **Waitlist verify (live).** With the API deployed and U3 pending: confirm a POST lands in the table; notifications stay OFF until U3.
- [ ] T2 **App regression sweep.** Repo checks green; `db:seed` twice → still exactly 24 with matching prices; `/public/properties` returns 24, CORS headers present, unauthenticated writes rejected; manual matrix (with user): t.me link on Android/iOS/Desktop → onboarding → testnet wallet → deep-linked property → buy sheet shows the RIGHT villa → cancel cleanly; sell sheet + orderbook render.
- [ ] T3 **Funnel assist.** Support the end-to-end testnet run (site → app): onboarding completes, buy sheet price correct, abort before transfer. Log timestamps.
- [ ] T6 **Sign-off.** All app-side boxes ticked → report to the user for launch sign-off.

## 6. Progress Log

> One dated entry per session. Never delete entries.

- **2026-09-12** — **Full e2e run to hunt substring-matcher false positives (user-directed; test-assertion hardening only).** Ran the complete Playwright suite (15 specs) to confirm no other spec carries the `getByText("APY")`-style false positive fixed in `income.spec.ts` (substring + case-insensitive matching once matched the concatenated page text "Villa du Cap|Your…" which contains "apy"). Static scan of all specs first: `getByText("APY")` asserted absent also in `home.spec.ts` and `marketplace.spec.ts`; other substring assertions (getByRole name, toContainText on testids/labels) are low-risk because they assert presence of known copy or target scoped containers. Full run result: **45 passed / 6 pre-existing conditional skips / 1 failure** — the failure was NOT a matcher issue: `order-lifecycle.spec.ts` reload step hit its `waitForSelector` 15s timeout under 2-worker parallel load (dev-server contention); it passes 3/3 standalone. Both risky `APY`-absent assertions passed as written, but carry the same latent hazard as the income.spec false positive, so both were hardened to the word-bounded `/\bAPY\b/` form (consistent with the established fix) and re-verified: home 2/2 + marketplace 3/3 green. **No other spec currently exhibits a substring false positive; the two remaining latent instances are now word-bounded.** Nothing committed.
- **2026-09-12** — **API seed-fixture canonicalization (user-directed follow-up to the web canonicalization session; identity/test data only — no payment/settlement/TON logic touched, no manifest edits, no DB schema changes).** Migrated `apps/api` off the legacy `prop-*` fixture ids to the canonical `re-<listingId>` scheme (mapping keyed 1:1 by fixture order/title via docs/PHASE-9-BASELINE.md §B, same as the web migration): (1) `db/seed/properties-data.ts` — all 24 fixture ids re-keyed (prop-marina-vista-4b→re-128862, prop-soho-loft-studio→re-126855, prop-bayside-marina-penthouse→re-108924, prop-alfama-terrace-flat→re-123861, prop-tbilisi-riverhouse-loft→re-125643, …prop-nyc-chelsea-loft→re-122113) + all 24 `rentalHistory` id prefixes retokenized to canonical ids; fixture economics/display data untouched (frozen-copy contract preserved; header documents the canonical-identity contract); (2) `db/seed/documents-data.ts` re-keyed incl. storage keys (`documents/re-128862/*`), and the runtime demo-documents guard in `index.ts` now checks `re-128862`; (3) admin create-property generated ids switched `prop-<slug>-<shortId>` → `admin-<slug>-<shortId>` (`re-*` reserved for canonical listing ids, `test-*` for fixtures) + its test expectation; (4) all ~30 API test files migrated: real-estate constants → canonical ids, synthetic/fallback ids → `test-*` (`prop-1`→`test-1`, `prop-a`→`test-a`, `prop-does-not-exist`→`test-does-not-exist`, holdingKeys `user:prop-x`→`user:test-x`, `prop-e2e-small-funding`→`test-e2e-small-funding`, s3 key `documents/prop-abc`→`documents/test-abc`); (5) `properties-data.test.ts` id-shape guard updated `startsWith("prop-")`→`startsWith("re-")`; (6) `apps/api/README.md` curl examples re-pointed to canonical urls. Three tests needed identity-coupling fixes, not logic changes: map-earnings same-week tiebreak order (propertyId locale order flipped with the rename), marketplace `?query=` smoke test re-pointed to a live fixture title ("marina" no longer exists in any title), public.test site-contract spot-check re-pointed to a row whose fixture location actually matches (Syrene/Italy/Sorrento, $120). Zero `prop-*`/`prop_*` matches remain anywhere under `apps/api` (source + tests + README). Untouched by contract: `portfolio-manifest.json` (site-shared ids, never rename) and the A3 manifest seed path (`manifest-data.ts`/`seed-properties.ts`) which already deletes legacy prop-* rows at seed time; `docs/` historical records keep old ids as history. **API suite 578/578 (66 files) ✅ · `typecheck:api` ✅ · web suite 1072/1072 unchanged.** Nothing committed.
- **2026-09-12** — **Property-identity canonicalization hardening (user-directed; identity/routing/tests only — no payment/settlement/TON logic, no manifest edits, no product decisions).** Finished the in-progress canonicalization: (1) `deep-link.ts` verified canonical-only (no `prop-` normalization remains — legacy params are rejected outright; comment truth-up only) and its test rewritten (`deep-link.test.ts`: all-24 `re_`/`re-` round-trips, `~utm` suffix, legacy `prop_*`/`prop-…` rejection, name-slugs are NOT ids); (2) every synthetic/fallback test id renamed to the `test-*` convention (`Header.test` prop-x→test-x, `getRepo.test` prop_dubai_marina_01→test-dubai-marina-01, `http-repos.test` prop_1→test-1 incl. the encoded-slash case) — the web app now has zero `prop-*`/`prop_*` id literals outside intentional rejection guards; (3) old display names removed from every test fixture that carried a canonical id ("Marina Vista Apt 4B"→Grand 2 BDM canonical name, "Bayside Marina Penthouse"→Syrene, "Alfama Terrace Flat"→Villa du Cap, "Tbilisi Riverhouse Loft"→Emerald Cay, etc.) and internal demo-seed record ids retokenized to canonical ids (`tx-re-108924-*`, `earn-re-108924-*`, `dist-re-123861-*`, `ord-open-re-123861-sell-1`, `lock_demo_re-*`, `nft-demo-re-*`, `PAYOUT_SYRENE`/`PAYOUT_VILLA_DU_CAP`, fixture `history()` prefixes → gallery slugs) with the one mirrored e2e testid updated (`other-secondary-ord-open-re-123861-sell-1`); (4) NEW global identity regression `src/lib/economics/__tests__/property-identity.test.ts` + test-data registry `src/lib/economics/estates/retired-names.ts` (24 retired names/slugs, keyed by canonical id, test-import only): asserts exactly 24 canonical `re-<listingId>` records with bidirectional lookups, seed ids == canonical ids, canonicalized view-model identity == canonical sourcing rule for all 24, ROUTES.property emits `/property/<canonical-id>` only, every canonical id round-trips the Telegram start param, every legacy `prop_*` param is rejected, and NO retired name/old slug/`prop-` string appears in any generated URL, deep-link param, or user-visible marketplace text (name/location/description/nightly/images); (5) stale `prop-*` comments truthed up in runtime files (canonical-24.ts, estate-24-data.ts, marketplace-view-model.ts, estate-canonical.ts). One pre-existing latent e2e false-positive found + fixed: `income.spec.ts` asserted `getByText("APY")` toHaveCount(0), but "Villa du Cap"+"Your income flow" concatenates to a text containing "apy" (Cap|Your) — matcher made word-bounded (`/\bAPY\b/`). Fixture `description` prose in `seed/properties.ts` intentionally left (dead data — toCanonicalListing overrides it for all 24; PropertyAbout is the only reader and is canonical-first). NOT touched (separate system, out of scope): `apps/api` seed/test fixtures still use legacy `prop-*` fixture ids (`properties-data.ts`, `documents-data.ts`, ~30 API test files) and `portfolio-manifest.json` keeps its own site-shared IDs — both are the API/DB truth, unaffected by the web identity layer. **Web suite 1072/1072 (127 files); `npm run check` exit 0 (lint 0 errors, 2 pre-existing warnings, typecheck clean, build green); e2e verified on touched surfaces @480×840: income 2/2 + order-lifecycle 3/3 + buy-flow + sell-flow + property-detail + estate-rtl 16/16 + estate-economics 8/8 (incl. all-24 sweep) + smoke-shell/earnings-honesty 6/6.** Nothing committed.

- **2026-09-02 (4)** — **Rebuild Slice B: Scenario Engine (user-directed per the Slice B execution prompt; pure engine + types + tests only — no UI, no ShareModel/PlanEngine changes, no product decisions resolved).** Context: after the forensic baseline, source-of-truth docs were materialized locally (`9475faa`) and Slice A's contract-required reconciliation artifact was added (`84ea82f`) — the one pre-B blocker from the A/C/D contract audit. Created `src/types/estate-scenario.ts` (ScenarioInput — optional occupancyRate/adrUsd as Provenance-typed ScenarioValue assumptions + averageOccupiedGuests number|null|undefined; omitted=inherited, provided=override for that evaluation only, null=force UNKNOWN; ScenarioResolution inherit-vs-override record; ScenarioEvaluationKind point|envelopeBound|baseline; ScenarioResult = composition: metadata + resolution + currency + THE EstateEconomics object itself (cost lines, totals, profit, allocation, provenance, reconciliation travel inside it — no duplicated fields that could diverge); ScenarioEnvelopeResult lower/upper never collapsed; NamedScenarioDefinition = configuration data only); `src/lib/economics/scenario-engine.ts` (ZERO arithmetic — orchestration above computeEstateEconomics: requireValidOccupancy 0..1 no clamping, requireValidAdr positive finite, resolveGuests non-negative integer / undefined=inherit / null=force-unknown; resolveScenario builds a FRESH EstateScenario context (Estate never mutated); evaluateScenario / evaluateBaselineScenario (canonical baselineScenario verbatim — no midpoint invention; unknown ADR → explicit Error, never guessed from the nightly range) / evaluateNamedScenario / evaluateOccupancyEnvelope + evaluateAdrEnvelope (explicit estate or caller bounds, both bounds through the EconomicModel, never averaged); re-exports the canonical entry point). estate.ts `EstateScenarioSet`/`EstateScenarioKind` placeholders replaced with estate-scenario re-exports; index.ts additive export. Tests `src/lib/economics/__tests__/scenario-engine.test.ts` — 40 tests: immutability (JSON snapshot across all evaluators), multi-scenario independence, determinism (deep-equal + JSON), STRUCTURAL model-reuse proof (result.economics toEqual direct computeEstateEconomics on the resolved context), occupancy points 0/0.6/0.7/0.8/0.9 monotonic + exact, 0%-occupancy zero-revenue/reserve-unchanged, Grand 2 BDM seed assertions + envelope-coherent gross ($16.1M–$24.1M at documented bounds), occupancy+ADR envelopes keep bounds distinct (midpoint gross ≠ either bound; ADR bounds never averaged), custom bounds verbatim, full output surface (all 6 cost lines, totalCosts, net/owner/operator/travelAgency), metadata kind/bound/currency/resolution, override-only-occupancy leaves ADR+guests inherited, write-back never happens, invalid occupancy/ADR/guests rejected (no clamping/flooring), unknown-ADR estate → explicit failure vs rescued by explicit override, missing guests keep honest-unknown green tax, null-guest override forces UNKNOWN even when estate configures guests (and inherited guests evaluate known — asserting Slice A's omit-when-known flag), bridged legacy estate evaluates honestly (zero revenue, unknown tax), named config scenario echoes id/label, reconciliation preserved verbatim in point/baseline/envelope results incl. honest-false when unknown. Two self-caught defects fixed during development: envelope helpers initially dropped the bound/kind metadata; one test asserted literal `unknown:false` where Slice A omits the flag when known. **Web suite 734/734 (93 files, was 694); `npm run check` exit 0.** Legacy conflicts: legacy yield/APY logic NOT revived or consulted (format.ts/yield-math.ts coexist untouched). Product decisions required: exact Conservative/Base/High ADR/occupancy defaults (NOT invented — named scenarios are configuration-data only); guest-count values per estate (green tax unknown at baseline); whether scenario outputs should carry a product-facing range narrative beyond the two bounds. Nothing else committed (pre-existing eslint.config.mjs + PropertyGallery.tsx still untouched-uncommitted).

- **2026-09-02 (3)** — **Rebuild Slice D: Investment Plan Engine (user-directed per the Slice D execution prompt; pure engine + types + tests only — no UI, no Buy flow, no income ledger, no payment/settlement changes).** Note: Slice B (Scenario Engine) still NOT implemented — PlanEngine built on the contract's optional-snapshot path (snapshot is opaque passthrough, never read/recomputed); the three `docs/product/rebuild/*.md` reference docs still do not exist in the repo (worked from the prompt's inlined contract, same as Slice A). Created `src/types/estate-plan.ts` (PLAN_TARGET_RATE_ENVELOPE 0.80–1.25; PlanConfig — optional id/label/principalMinimum/principal + explicit targetProfitRate/termMonths/payoutCadence(monthly|weekly)/distributionMode/startDate + optional opaque PlanEstateSnapshotRef; PlanEvaluationOverrides for what-if without preset mutation; PlanProjection — ProvenancedValue targetProfit/targetTotalReturn + numberOfPeriods/periodProfitUsd/schedule nullable with explicit scheduleOmittedReason missingStartDate|cadenceNotSchedulable); `src/lib/estate-plan-engine.ts` (pure deterministic, integer cents half-up once: targetProfit=principal×rate, total=principal+profit; validatePlanConfig→PlanValidationIssues + hasPlanIssues pattern mirroring Slice C, evaluatePlan {ok|issues} + evaluatePlanOrThrow; envelope "approved" default + explicit "widerForTests" flag; **period-count rules: monthly→termMonths PROVISIONAL flagged for product confirmation, weekly NOT derivable (no repo calendar rule) → totals only**; only distributionMode "evenProfitPerPeriod" (contract example) — unknown modes rejected, never silent-defaulted; even split = floor + 1-cent remainder to EARLIEST periods so Σ schedule = targetProfit exactly; dates = startDate+index only (no wall clock); MAX_TERM_MONTHS=MAX_SCHEDULE_PERIODS=600 engineering guards; unsafeAmounts explicit on >MAX_SAFE_INTEGER; cumulative/remaining helpers = closed-form of the same split; planPrincipalFromShares = shares×primarySharePrice via the Slice C helper — the ONLY share integration). estate.ts placeholder `EstatePlanEngine = never` replaced with real type re-exports; types/index.ts additive export. Tests `src/lib/__tests__/estate-plan-engine.test.ts` — 54 tests (TEST-fixture config labeled, no production presets invented): 80%/1.00/1.025-mid/125% + inclusive envelope bounds, half-up rounding boundaries, remainder boundaries (1c÷3, Σ=cumulative chain), terms 6/12, cadence rules + weekly/missing-startDate omission reasons, principal 0/negative/fractional/missing/huge-overflow/principalMinimum, out-of-envelope rejection + widerForTests flag, unknown cadence/mode/date rejection, PROJECTED provenance + no paid/accrued vocabulary, determinism + config/override non-mutation, opaque snapshot passthrough (math identical with wild snapshot values), ShareModel bridge incl. unconfigured-price failure. One self-caught test defect fixed during development (mis-scaled cent expectation in the shares→principal end-to-end test; engine math was correct). **Web suite 688/688 (92 files, was 634); `npm run check` exit 0 (typecheck clean, lint 0 errors, build pass).** Legacy conflicts: legacy `yield-math.ts` (lock installments) and `planWithdrawal` are unrelated lock/withdrawal math — NOT revived as PlanEngine, coexist untouched; no legacy plan-card code existed. Product decisions required: production plan presets (rates/terms set); confirmation of monthly→termMonths period rule; weekly period-count calendar rule (or defer weekly); distributionMode alternatives beyond evenProfitPerPeriod; startDate policy (fixed day vs purchase-anchored); principalMinimum values. Nothing committed — awaiting commit instruction alongside pre-existing modified eslint.config.mjs + PropertyGallery.tsx and uncommitted Slice C.

- **2026-09-02 (2)** — **Rebuild Slice C: Share Model (user-directed per the Slice C execution prompt; pure engine + types + tests only — no UI, no PlanEngine, no Buy/Sell flows, no orderbook revival).** Note: Slice B (Scenario Engine) was NOT implemented (its session died before any files landed); Slice C built on the contract's anticipated optional-snapshot path. Created `src/types/estate-share.ts` (EstateShareConfig — explicit totalShares/primarySharePrice/primarySharesAvailable + optional nearlySoldOutThreshold product-decision field + nullable ProvenancedValue estateValue; EstateSecondaryListing minimal shape; EstateUserPosition with per-share avgCost mirroring legacy Holding convention; EstateShareStructure with nullable ownershipPerShare/referenceAssetValuePerShare; RESTRUCTURED state model — EstatePrimarySupplyState available/nearlySoldOut/soldOut flags + mutually-exclusive EstateShareMarketState label: tests exposed that collapsing "primarySoldOut" and "noSharesAvailable" into one enum slot was wrong since sold-out is a primary-market fact independent of secondary availability; flag-level truth + honest label); `src/lib/estate-share-model.ts` (pure deterministic helpers: validateEstateShareConfig; ownershipPerShare 1/totalShares guarded; ownershipForShares; referenceAssetValuePerShare = estateValue/totalShares half-up-rounded, UNKNOWN when value missing — never presented as market price; primaryPurchaseTotalUsd explicit failure when price unconfigured; secondarySaleTotalUsd; gainLossVsAcquisition gain/loss/breakEven with explicit failure on unknown acquisition basis; remainingOwnershipAfterSale rejects oversell; primarySupplyAfterPurchase rejects overbuy with explicit error — no silent caps; deriveShareState data-driven with nearlySoldOut ONLY when threshold configured (no magic numbers); secondaryMarketPrice no-invention rules: configured mark wins → exactly-one-active-listing ask (observed) → else null UNKNOWN, lowestActiveAskUsd exposed as quoted fact, never blended across multiple listings; estateShareOverview; attributeOwnerProfitToShares = snapshot.ownerProfit × ownershipRatio via the Slice A EconomicModel — NO new profit formulas — null when netProfitKnown false; attributeBaselineOwnerProfit convenience delegating to computeBaselineEstateEconomics). estate.ts placeholder `EstateShareModel = never` replaced with real type re-exports; index.ts additive export. Tests `src/lib/__tests__/estate-share-model.test.ts` — 48 tests (TEST FIXTURE config labeled as test configuration, no production supply invented): structure derivations incl. divide-by-zero guards, purchase totals 0/1/many + unconfigured-price failure, gain/loss/break-even, remaining ownership incl. oversell rejection, supply projection incl. overbuy rejection, config validation, states (available/nearly-sold-out gated/sold-out-flag/secondary-path/no-shares-flag/listing-status filtering/ownership flags), secondary no-invention (none/one/many/configured-mark/three-distinct-prices fixture), attribution via EconomicModel (null when guests unknown), determinism, no-mutation. Two self-caught defects fixed during development: require() → static ESM import; wrong ownershipForShares argument in attribution convenience. **Web suite 634/634 (91 files, was 586); typecheck clean; lint 0 errors (5 pre-existing warnings); build pass.** Legacy conflicts: legacy Holding/OrderBook/Trade remain the trading-layer model (untouched, coexists); legacy `Listing.totalShares`/`sharePriceUsd` are the legacy equivalent of share config — canonical config is a separate explicit input, bridge is a later-slice decision. Product decisions required: per-Estate totalShares/primarySharePrice/primarySharesAvailable production configuration; nearly-sold-out threshold; secondary-price aggregation rule across multiple listings; capital→shares rounding rule (not implemented — no repo rule found); Slice B still pending (attribution tested with fixtures). Nothing committed yet — awaiting commit instruction alongside pre-existing modified eslint.config.mjs + PropertyGallery.tsx.
- **2026-09-02** — **Rebuild Slice A: Economic Data Architecture (user-directed per the Slice A execution prompt; architecture-only — no UI, no legacy rewrites, no financial/settlement changes).** Created the canonical Estate economic model + the ONE calculation layer: `src/types/estate.ts` (ProvenancedValue with observed/estimated/calculated/projected/unknown; EstateAsset with propertyValue/nightly range/occupancy bounds/explicit optional ADR/asset grossAnnualRevenue; ESTATE_COST_RATES + ESTATE_ALLOCATION_RATES constant tables; EstateCostLine with basis + unknown flag; EstateScenario with configurable occupancy + explicit nullable averageOccupiedGuests; Estate + EstateEconomics result; ScenarioSet/ShareModel/PlanEngine placeholders for Slice B+); `src/lib/economics/estate-economics.ts` (pure deterministic, integer cents, half-up rounding per line; documented ADR rule #1 explicit-ADR else #2 nightly-range midpoint provenance estimated; baseline occupancy = estate's own bounds midpoint; occupiedNights=365×occ; gross=ADR×nights; the six cost lines — tourism 17%/service 10%/OTA-agency 18%/operator 12.5% of gross, green tax guest-nights×$12 with missing-guests → line unknown:true + netProfitKnown:false (never guessed), reserve 1.5% of property value; net=gross−Σrounded lines; owner 40%/operator 60% of net; travel-agency share 18% of GROSS kept a structurally distinct field from the 18% agencyRentalOta COST line; computeEstateEconomics single entry; legacy-bridge `estateFromLegacyListing` + rate parser, additive only); `src/lib/economics/estates/grand-2-bdm-ocean-pool-villa.ts` (canonical seed: $8M estimated, $67k–$80k, 60–90%, baseline ADR $73,500/occupancy 75% midpoints, guests null, asset grossAnnualRevenue self-assigned from the canonical layer, 33 existing JOALI images, rebuild-namespace id NOT a manifest propertyId); exported via `src/types/index.ts` (additive). Tests `src/lib/economics/__tests__/estate-economics.test.ts` — 42 tests: asset values/ranges/ADR-not-min-max, all six cost formulas, green tax guest-nights + capacity-independence, net/40/60, the two-18% distinctness, provenance validity, fixture integrity (no NaN/undefined, registry=1, safe integers, integer cents), edge cases (0%/100% occupancy, missing guests, zero property value, min/max nightly ADR, explicit-ADR override + midpoint fallback), legacy bridge non-invention. Found+fixed during testing: scenario-ADR was ignored by computeEstateEconomics (always estate baseline) — fixed via `scenarioAdrUsd`. **Web suite 586/586 (90 files, was 543); `npm run check` equivalent green: typecheck clean, lint 0 errors (5 pre-existing warnings), build pass.** Legacy conflicts mapped but NOT refactored (additive slice): legacy `Listing`/`annualRentUsd`/`monthlyYieldRate` model = trading-layer assumptions (rate-applied-to-investment yield), separate from estate operating economics — coexists via the bridge; mandatory fields (photos/name/location/nightly rate) retained. Known gaps: rebuild prompt's three reference docs (`docs/product/rebuild/*.md`) do not exist in the repo (worked from the prompt's inlined contract); `$16M–$24M` gross band is a test-validated band, not a stored field; averageOccupiedGuests unresolved (green tax unknown at baseline). Product decisions required: ADR methodology confirmation, averageOccupiedGuests, occupancy/ADR per non-seed estates, provenance confirmation, canonical-estate↔manifest-ID linkage. Nothing else committed (unrelated pre-existing working-tree changes to `eslint.config.mjs` + `PropertyGallery.tsx` left uncommitted). Slice A committed as `f2ab16e` on `phase-9-redesign` (not pushed). Slice B (Scenario Engine) ready to build on this foundation.
- **2026-09-01 (4)** — **Phase 9 Slice 5 formal acceptance (independent acceptance audit; docs-only).** Verdict: **ACCEPTED WITH NON-BLOCKING FINDINGS**. Evidence re-verified by the auditor, not taken from the implementation report: git ground truth (branch `phase-9-redesign`, HEAD == `origin/phase-9-redesign` == `e4db4e1`, working tree clean, `main` untouched); scope audit of the full 29-file Slice-5 range (implementation = earnings page/components/lib + i18n ×12 + e2e + screenshots + tooling script + checkpoint docs; zero financial/route/shell/API/package files); unit **543/543 (89 files)** across 4 consecutive full runs (one intermittent pre-existing flake in `PropertyDetail.test.tsx` async-chart `findByTestId` waits under parallel load — passes isolated, passes at the pre-Slice-5 commit `22bfcb8`, not Slice-5-related); `npm run check` exit 0 (typecheck clean, lint 0 errors + 5 pre-existing warnings, build pass); e2e @480×840 income 2/2 + earnings-honesty 2/2 + home 2/2 + marketplace 4/4 + property-detail 4/4 + smoke-shell 4/4 (portfolio 2/3 — confirmed pre-existing stale copy check on an untouched file); data-honesty trace clean (received=paid-only, Expected=pending-only hidden-not-$0, accrued=lock accrual, no weekly→monthly conversion on the surface); `earnings-honesty.spec.ts` contradiction resolved — the old spec asserted a PAYOUT_DISCLAIMER string the earnings page deliberately never renders (guaranteed pre-existing failure; the rewrite hard-fails 4 forbidden patterns including “guaranteed”, stricter than the old soft checks, and demo disclosure persists via the global `DemoModeBadge`); i18n 12/12 parity (36 `earnings.*` keys), removed keys referenced nowhere. Non-blocking findings: chart keeps 12-week weekly bars vs §7.2 “monthly 6/12/all” — documented deviation rooted in the §7.3 [AUDIT-OVERRIDE] (monthly conversion of paid events violates rules 2–3), revisit on formal weekly-vs-monthly resolution; mixed paid+pending week buckets render in paid color (legend explains palette); bottom-nav “Earnings” vs H1 “Income” → Milestone 9.1. Pre-existing/deferred: `portfolio.spec` stale copy test (Slice 6 surface), `PropertyDetail.test` async-chart flake, 6 dead `earnings.*` keys, money-path `BASE_URL` gating, prior deferred findings 1–4. Acceptance recorded in plan §13 row 5 + §14; docs-only commit, no code changes. Not merged to `main`. Slice 6 NOT STARTED.
- **2026-09-01 (3)** — **Phase 9 Slice 5: Income (`/earnings`) — rental-income reframe (per `PHASE-9-PRODUCT-REDESIGN.md` §10 + UI Mapping §7; contract slice 5; Slice 4 formally ACCEPTED WITH NON-BLOCKING FINDINGS first, docs commit `a6b20b1`).** Reframed the Earnings surface as **Income** honoring the §7.3 weekly-vs-monthly conflict UX rules (status words only — never frequency promises): page H1 **Income** + subtitle “Your share of the rental income from every estate you own.”; hero relabeled **“Received in total”** (paid money only, was “Total Earned”) with the Pending/Paid hero pill removed and the next row labeled **Expected** (was “Est.”; pending-entries only, hidden when no pending — never fake $0); **NEW accrued block** — locked-share card reframed to **“Accrued income”** with the accrued-unpaid figure leading + “Accrued, paid with next distribution” caption, “Projected monthly” label on the monthly figure (§7.3 rule 2); chart keeps static 12-week bars (actual paid vs projected pending two-tone) + **NEW Paid/Projected legend** + caption replaced with the one honest line “Distribution schedule is being aligned with the monthly income model.” (rule 5); timeline rows now **Paid (actual amount + actual week) / Accrued (repo accrued-unpaid, hidden when no lock data) / Expected (pending + Sunday display date)** — period-claim copy removed (“current month”, “Monthly accrual; payouts reconcile every Sunday.”); **NEW income-by-estate** block (P0): paid-only per-estate totals grouped by `propertyId` (first-appearance order, estates without paid entries simply absent), each row links to `/property/[id]` (names/images from the existing marketplace contract via `useMarketplace` — no API change); withdrawal entry mechanics + locked copy untouched; empty-state copy de-periodized (“Buy your first share and start earning.”). i18n: `earnings.*` reframed/added across all 12 locales (10 new keys: title/subtitle/expected/timelineAccrued(+Sub)/yieldAccruedSub/chartLegendPaid/Projected/byEstateTitle/Received; 6 value updates; 3 period-claim keys removed; parity verified via `scripts/phase9_slice5_i18n.py`). Tests: **543/543 web** (was 539; +groupIncomeByEstate, +IncomeByEstate, +timeline status-word tests); `npm run check` exit 0 (lint 0 errors, 5 pre-existing warnings); typecheck clean; build green; e2e income 2/2 + earnings-honesty 2/2 (rewritten — old spec was stale: it asserted a PAYOUT_DISCLAIMER the earnings page deliberately never renders) + home 2/2 + marketplace 4/4 + property-detail 4/4 + smoke-shell 4/4 @ 480×840; screenshots in `screenshots/phase9-slice5/`. NO financial/settlement/orderbook/TON/route/shell/API changes (diff-reviewed; forbidden-area scan clean). Documented deviation: chart stays 12-week weekly bars rather than UI Mapping §7.2 “monthly 6/12/all” — monthly conversion of paid events would violate §7.3 rules 2–3; revisit on formal weekly-vs-monthly resolution. Pre-existing (not slice-caused): `portfolio.spec` “shows holdings or empty state” checks copy the portfolio page doesn't render (Slice 6 surface); money-path specs skip without `BASE_URL`. Committed + pushed to `origin/phase-9-redesign` (see §14 checkpoint in `PHASE-9-IMPLEMENTATION-PLAN.md` for the SHA). Not merged to `main`.
- **2026-09-01 (2)** — **Phase 9 Slice 4: Estates (`/marketplace`) — ownership-first reframe (per `PHASE-9-PRODUCT-REDESIGN.md` §6 + UI Mapping §4; contract slice 4).** Estates H1 + subtitle “Own a share of exceptional properties.”; search placeholder “Search villas, destinations or regions.”; filters exactly **All / Featured / New / Income / Owner Stay / Resale**; default sort **Curated** (stable manifest feed order — never highest yield) with **Rental income / Entry price / Newest** options (owner-privileges sort omitted: no entitlement data exists). Estate card: APY + Hot/Almost-Sold scarcity badges removed; metrics = Price (or Last price on resale) + Projected income / share with an honest **“Data pending”** ⓘ chip when income data is missing (never 0/fabricated); ownership fraction **“1 share ≈ 1/N of the estate”**; availability (“X% funded · N shares remaining” + bar) on primary offerings only; whole-card navigation preserved, no per-card Buy. **Deferred finding #1 resolution:** no featured flag or editorial ranking exists in any data layer (Listing type, mock seed, 24-property manifest) → the Estates *Featured* filter renders but matches nothing with the honest “Featured curation is not available yet.” empty state (same treatment as Owner Stay); `pickFeaturedListing` (Home) left unchanged and documented as needing real editorial data before replacement. New `estates.*` i18n namespace mirrored across all 12 locales (parity verified; marketplace.* trimmed to the fees* keys still used by FeeInfoButton/FeeScheduleSheet). Tests: **539/539 web** (was 526); `npm run check` exit 0 (lint 0 errors, 5 pre-existing warnings); typecheck clean; build green; e2e marketplace 4/4 + home 2/2 + estate-detail 4/4 + smoke-shell 4/4 @ 480×840; screenshots in `screenshots/phase9-slice4/`. NO financial/settlement/orderbook/TON/route/shell changes (diff-reviewed). Committed + pushed to `origin/phase-9-redesign` (see §14 checkpoint in `PHASE-9-IMPLEMENTATION-PLAN.md` for the SHA). Not merged to `main`.
- **2026-08-30 (6)** — **UI polish #08: bottom-padding audit on Property detail (user-directed; presentation only).** Measured with a new Playwright audit (`scripts/phase8-bottom-space-review.mjs`, 480x840 TG viewport, primary+secondary, all 5 tabs): every tab ended with a uniform **272–276px blank tail** after the last content. Cause: stacked padding — page wrapper `pb-40` (160px, from #05) + `PropertyDetail` trailing `pb-6` (24px) + AppShell global bottom inset `pb-[calc(88px+safe-area)]` (88px) = 272px; no spacer/min-height/flex-grow existed. Fix: page wrapper `pb-40`→`pb-24` (96px = 52px sticky CTA + scrim + gap) and removed `PropertyDetail`'s trailing `pb-6`; the AppShell 88px inset remains as the intentional Telegram-chrome clearance. Result: uniform 184–188px tail, verified by re-running the audit; nothing hidden behind the CTA (hiddenBehindCta=0 on all 10 tab/property combos, owning and non-owning). Applies to all 5 tabs and both Primary/Secondary (single shared wrapper). Tests: property suites 126/126, tsc clean, lint 0 errors, build green. Nothing committed.

- **2026-08-30 (5)** — **UI polish #07a/#07b: Market summary price row + compact property top bar (user-directed; presentation only, no financial logic).** #07a: `MarketSection` current-price row reflowed to one left-to-right line — label "Current price", price value, then vs-offer delta — all at the label's 11px size (the 18px bold price was the wrong hierarchy per the latest screenshot); `flex-wrap` keeps it mobile-safe; best bid/ask/spread row untouched. #07b: new `PropertyCompactTopBar` (back chevron + property title) revealed by the new `useScrollDirection` hook (direction-aware: scroll-up only, 320px threshold ≈ past the hero, 4px jitter filter, rAF-throttled); fixed overlay, no layout shift, hides while sheets/settings are open; `.compact-topbar` CSS transition added to globals.css (reduced-motion honored). Wired in `property/[id]/page.tsx`. Tests: property suites 116/116 + page 10/10, tsc clean, lint 0 errors (1 unused-import warning fixed during dev, 3 pre-existing API warnings), build green. Nothing committed.

- **2026-08-30 (4)** — **UI polish #02/#05: Property detail hero badge removal + sticky-CTA spacing (user-directed; no financial logic touched).** #02: removed the Primary/Secondary market badge chip from `PropertyHero` (the "Secondary Market"/"Primary Offering" pill) — the funding/live-trading status banner and the Overview tab remain untouched; unused `cn` import cleaned up. #05: sticky Buy/Sell bar no longer sits flush against the content above — page-level clearance under `PropertyDetail` raised `pb-24`→`pb-40` (96→160px, covers the 52px bar + safe-area + gradient scrim) in the property route, and the sticky bar's top padding `pt-3`→`pt-6` widens the fade-out scrim so short tab panels (Holders/Income) keep a visible gap; holds on all 5 tabs. Tests: 510/510 web, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-30 (3)** — **UI polish #01: Marketplace card label — "Min. purchase" → "Night / From" (user-directed; marketplace feed cards only, no Property detail changes).** `PropertyCard` third metric label switched from `minPurchase` to new `marketplace.nightFrom` key ("Night / From"), value unchanged (`usd(displayPrice)`). Applied to all card variants — Primary and Secondary use the same metric grid; Secondary's first metric stays "Last price" per PD-07. `nightFrom` mirrored across all 12 locales; old `minPurchase` key removed everywhere (kept in en marketplace section as documented no-op for parity tooling). Tests: updated `PropertyCard.test.tsx` label assertion. 510/510 web, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. No financial logic touched. Nothing committed.
- **2026-08-30 (2)** — **Property redesign Phase 8: Details & final polish (user-directed, per docs/design/PROPERTY_REDESIGN.md §PHASE 8; resumed from partial Phase 8 — Phases 0–7 untouched).** Details tab polish: `PropertyDocumentsList` localized (title/empty/kind/error now use `property.*` keys instead of hard-coded English) and download rows got `aria-label`s (`Download {title}`); Documents block renders only when `onDownloadDoc` is wired (pre-existing behavior kept). Localization: parity check of the `property` namespace across all 12 locales — the 11 non-en locales were already at parity, so only the new keys were added (7 documents/trend keys + 3 a11y keys, mirrored across all 12). A11y: screen-reader trend summaries added to Secondary Performance chart (`chartTrendSummary` with `trendUp`/`trendDown`, start → end, so up/down is not color-only), Primary funding + cumulative charts, and Income chart (cumulative per-share total), all `sr-only` and wired via `aria-describedby`. Performance: the four heavy analytics panels (HolderAnalytics, IncomeAnalytics, Primary/Secondary PerformanceCharts) are now lazy-loaded via `next/dynamic` (`ssr: false`) with a matching `TabPanelSkeleton` fallback — the Overview (default tab) ships without their chart JS. Yield-mode axis context (gross-yield dashed reference line + caption) was already present in SecondaryPerformanceCharts; Primary end labels were already quiet captions — no change needed. NO financial/settlement/lock/sell/yield/fee logic touched (page.tsx analytics data flow untouched; only presentation/i18n/a11y/lazy-loading). Tests: 510/510 web (no count change — updated async assertions to `findByTestId` in PropertyDetail.test + property-consistency.test), lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-29 (7)** — **Property redesign Phase 7: Income Analytics (user-directed, per docs/design/PROPERTY_REDESIGN.md §13).** New `IncomeAnalytics` (`src/components/property/IncomeAnalytics.tsx`) replacing the Phase 1 `income-coming-soon` shell on the Income tab: main chart = 12 monthly per-share payout bars (Telegram blue, muted when unselected) + cumulative per-share yield line (finance green, right-scaled) from the Phase 4 shared `incomeHistory` dataset (ends at current month, anchored to annualRentUsd/12 ±3%); tap-a-bar tooltip via existing HitZones (month, per-share, pool). Payout history list (month, payout date, per-share, whole-pool, newest first, hairline separators, simulated-data disclosure). Real-estate metrics block shows ONLY the two ratios the shared metrics actually carry — gross yield and annual yield rate — with an explicit note that net yield, cap rate, NOI and expense ratio are NOT shown (dataset lacks those fields; not fabricated). Projections block points at the existing Overview `IncomeCalculator` (untouched, labelled projections-not-promises). Works for both Primary and Secondary (incomeHistory is generated for both; no price chart involved). 13 new `property.*` keys mirrored across all 12 locales. NO financial/settlement/lock/sell/yield/fee logic touched (page.tsx untouched; analytics lib untouched). Tests: new `IncomeAnalytics.phase7.test.tsx` (5 tests incl. cumulative-total===Σper-share, newest-first rows, ratio values, projections pointer, tap tooltip) + updated PropertyDetail.test (income tab now asserts Phase 7 analytics); **510/510 web** (was 505), lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.

- **2026-08-29 (6)** — **Property redesign Phase 6: Holder Analytics (user-directed, per docs/design/PROPERTY_REDESIGN.md §12).** Six visualizations on the Holders tab, all consuming the Phase 4 shared holder/ownership datasets (anonymized buckets, no PII), all built on the Phase 5 chart primitives: `HolderDonut` (top-5 buckets + Others, center = Total Holders, tap tooltip), `TopHoldersBar` (ranked horizontal bars with rank/label/shares/%), `OwnershipTreemap` (slice-and-dice, in-rect labels when the rect is big enough, tap tooltip), `DistributionOverTime` (stacked bands over 52-week ownershipHistory, 1M/3M/6M/1Y/ALL range pills, per-week tap tooltip), `HolderBubbleChart` (bubble AREA ∝ shares via r=√area, deterministic golden-angle spiral placement, tap tooltip), `HolderStats` (6-cell statistics grid; every value from the same dataset). Shared categorical palette `HOLDER_COLORS` (calm opacity ramp of the one Telegram-blue accent) + `OTHERS_COLOR` in charts/shared.tsx. PropertyDetail Holders tab: Phase 1 "coming soon" shell replaced by `HolderAnalytics`; works for both Primary and Secondary (holder data exists for both). 15 new `property.*` keys mirrored across all 12 locales. NO financial/settlement/lock/sell/yield/fee logic touched. Known limitation: "Top 10 ownership" from the spec is shown as Top 5 because the shared dataset has only 6 anonymized buckets (no invented data). Tests: 111 property tests incl. Phase 6 suites; lint 0 errors, tsc clean, build green. Nothing committed.

- **2026-08-29 (5)** — **Property redesign Phase 5: Performance / Market Analytics (user-directed, per docs/design/PROPERTY_REDESIGN.md; resume after provider session drop — prior Phase 5 attempt was NOT started, working tree was clean of Phase 5 work).** New shared SVG chart primitives (`src/components/property/charts/shared.tsx` — viewBox constants, buildPaths, GridLines, DateLabels, HitZones, RangePills; token-only colors, touch-friendly). New `PrimaryPerformanceCharts` (Funding Progress Over Time + Cumulative Shares Sold, both from the Phase 4 shared `fundingHistory` dataset ending exactly at current sharesSold/progressRatio; timeframe pills 1M/3M/6M/1Y/ALL; yield projection block showing ONLY existing metrics — annualRent + grossYield, no new financial math; STRICT §10: no price series anywhere on Primary). New `SecondaryPerformanceCharts` (main chart from shared `priceHistory`/`ohlc` — line/area + candlestick since Phase 4 OHLC exists; volume underlay under price mode; Price↔Yield switch with yield derived from the SAME walk via annualRent÷(totalShares×price); 5 timeframes all backed by 52 weeks; green-up/red-down finance semantics; anchor = getCurrentSharePrice, series ends exactly at it). `PropertyDetail` performance panel rewired: Primary = fixed-price note + new funding charts (old placeholder note removed); Secondary = new charts replace legacy `PerformanceChart` (which remains untouched on disk for potential reuse). 7 new `property.*` i18n keys mirrored across all 12 locales. NO financial/settlement/lock/sell/yield/fee logic touched (page.tsx untouched; analytics lib untouched). Tests: new `PerformanceCharts.phase5.test.tsx` (8 tests incl. strict no-price-chart-on-Primary + end-price===anchor) + updated PropertyDetail.test (3) + property-consistency.test (2); **505/505 web** (was 497), lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-29 (4)** — **Property redesign Phase 4: Analytics Data Foundation (user-directed, per docs/design/PROPERTY_REDESIGN.md).** New `src/lib/property-analytics.ts` — the ONE shared deterministic dataset per property (FNV-1a + mulberry32, same spirit as performance-series): 52-week secondary price+volume series ending EXACTLY at getCurrentSharePrice(), weekly OHLC derived from the same walk (opens chain close→open, bands bracket open/close), ~26 historical fills sampled from the same points, 6 anonymized holder buckets (Σ shares === totalShares, Σ bps === 10_000 exactly), 52-week ownership history ending verbatim at the current distribution, 52-week Primary funding history (monotonic cumulative sharesSold, ends exactly at current sharesSold/progressRatio, NO price series for Primary), 12-month monthly income history anchored to annualRentUsd/12 (±3%, per-share floor), and metrics derived only from existing listing fields. Selectors: `sliceRange` (1M/3M/6M/1Y/ALL), `priceToOhlc`. NO financial/settlement/lock/sell/yield/fee logic touched; no chart UI (Phases 5–7); no new deps; no i18n keys needed (no user-facing strings this phase). New integrity tests: `src/lib/__tests__/property-analytics.test.ts` (26 tests — determinism, totals/percentages, date order, final/current alignment, Primary fixed price, measured variation bounds). Tests: 497/497 web (was 471), lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-29 (3)** — **Property redesign Phase 3: Secondary Overview (user-directed, per docs/design/PROPERTY_REDESIGN.md; Phase 2 approved same day).** New `PositionCard` (§9 Your Position: total/locked/free shares, accrued unpaid from active locks — display-only sum wired through page.tsx, estimated value at the single-source current price; Lock/Sell buttons open the EXISTING LockSheet/SellSheet; hidden when user owns nothing; replaces OwnershipBanner on secondary). `MarketSection` upgraded with a market-summary block (current price via lib/property-price, vs-offer delta ONLY when a last trade exists — no simulated history, best bid/ask, spread when both sides exist) and moved from Performance tab to the Overview lead; Performance tab keeps PerformanceChart only (book/trades now on Overview). Secondary Overview hierarchy: market summary → position card → calculator → yield/lock. Primary Overview (Phase 2) unchanged; Primary still renders NO price chart. 16 new `property.*` keys across all 12 locales. NO financial/settlement/lock/sell/yield/fee logic changed; no simulated datasets; no new deps. Tests: 471/471 web, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-29 (2)** — **Property redesign Phase 2: Primary Overview (user-directed, per docs/design/PROPERTY_REDESIGN.md; Phase 1 approved same day).** New Primary-only components: `FundingPanel` (calm % funded headline + FundingBar + translated "X% funded · N shares remaining" caption + rows sold/total, remaining, total target, fixed offer price — no urgency/FOMO/fake scarcity) and `PropertyFundamentals` (existing data only: property value, annual rent, gross yield = annualRent÷totalValue; nothing fabricated). Primary Overview hierarchy: Funding panel → ownership banner → calculator → fundamentals → yield/lock → trust; Secondary Overview unchanged (Phase 3 territory). Primary Performance tab note now fully next-intl localized (primaryFixedPrice/primaryFundingNote). Details tab renders trust only for Secondary (Primary carries it on Overview). 14 new `property.*` keys mirrored across all 12 locales. NO financial/settlement/lock/sell/yield/fee logic touched; IncomeCalculator used as-is; no simulated datasets (Phase 4). Tests: 464/464 web, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-29** — **Property redesign Phase 1: foundation (user-directed, per docs/design/PROPERTY_REDESIGN.md; Phase 0 audit approved same day).** Tab architecture landed: new `PropertyTabs` (5 tabs, horizontal scroll, roving-tabindex a11y, `no-scrollbar` utility in globals.css) + `PropertyDetail` rewritten to header (gallery+hero) → KPI grid → tab panels. Strict spec rule enforced: Primary never renders PerformanceChart (calm fixed-price note instead); Secondary keeps chart + MarketSection inside Performance. Holders/Income are honest "coming soon" shells (no simulated datasets — Phase 4). Header gained a Primary/Secondary market badge; KPI price label branches to "Offer price" on primary + shares sold/total added. i18n: 11 new `property.*` keys mirrored across all 12 locales (pre-existing hard-coded English in section components deliberately NOT swept this phase). NO financial/settlement/lock/sell/yield/fee logic touched (verified: page.tsx untouched; only composition/labels changed). Tests updated + new tab/branching coverage: 462/462 web tests, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-28 (2)** — **Task 3: final interaction polish — cohesion pass (user-directed, outside the A/I checklists).** Shared page transition: `dh-page-in` (180ms fade + 6px rise, reduced-motion-aware) applied via a pathname-keyed wrapper in AppShell — one pattern for all routes, no per-component animations. Back-button stack unified: SettingsSheet now consults `closeTopSheet()` (nested language/about/sign-out/withdraw/disconnect-confirm sheets) with an explicit guard so a pending disconnect can never be dismissed by Back (its sheet is unregistered while non-dismissible); Transactions page consults the registry before `router.back()` so a global sheet opened over it owns the press. Buy errors: inline sheet error kept (usability decision — error sits at the point of decision), page-local toast implementation removed in favor of the global `pushToast` (no parallel system). Dead UI removed: BuyQtyStep fake "Wallet balance — shown when chain linked" row deleted (TonConnect exposes no balance; honesty over decoration) + stale "weekly rental yield" connect-copy and layout.tsx metadata made frequency-neutral per the locked monthly model. Download errors now surface in the Documents block (error prop was defined but never passed). Tests: AppShell transition test, Settings back-stack unwinding + protected-disconnect tests, BuyQtyStep honest-UI assertion — 459/459 web tests, tsc clean, lint 0 errors (3 pre-existing API warnings), build green. Evaluated and skipped: sheet exit animation (fights the new set-state lint rules + conditional-mount architecture for marginal gain). Nothing committed.
- **2026-08-28** — **Task 2: interaction-quality pass — "make the app feel alive" (user-directed, outside the A/I checklists).** Audit-then-implement; NO visual redesign, NO financial/settlement/NFT/DB logic changes (verified: only UI/hook layers touched; all money math still in types/fees, lib/yield-math, lib/mock/withdrawals mirrors). New primitives: `ConfirmActionSheet` (generic intent→details→confirm→loading→success/error-retry sheet), global single-slot toast (ui.store + `ToastHost` in AppShell), `Sheet.dismissible` prop + sheet close-registry (`closeTopSheet`) so the TG BackButton closes the topmost sheet instead of abandoning flows. Flows: **Unlock** now requires confirmation (property/shares/period/accrued → Confirm unlock → Unlocking… → success; no mutation before confirm — the P0 prototype example), **Instant sell** got a review step + completion state (irreversible −7% buyback no longer one tap), **Withdrawal** got an itemized review (1% fee, net, 4 weekly installments, address — preview reuses `planWithdrawal` mirror of API math) + success state, **Cancel order** + **wallet disconnect** got confirmation sheets, **LockSheet/SellSheet** got success states instead of silent closes, limit-buy/custom-sell placement confirms via toast, CSV export no longer swallows errors (toast + catch). LockSheet/SellSheet in YieldLockSection/OwnershipBanner now mount conditionally (G10) — removes react-hooks/set-state-in-effect lint errors at the root. Tests: 5 new/updated files (ConfirmActionSheet, unlock flow, SellSheet, ToastHost, Withdrawal review flow) — 453/453 web tests, lint 0 errors (3 pre-existing API warnings), tsc clean, build green. Nothing committed.
- **2026-08-26** — **Collectible Position NFT system (user-directed, outside the A/I checklists).** DB `holding_nfts` (migration 0029, 1 holding → 1 NFT unique constraints), settlement hook in `settleVerifiedBuy` (best-effort, never blocks the buy), BullMQ worker with guarded lifecycle (pending → minting → minted → transferring → delivered, +failed retryable) and a recovery sweep (stale pending re-enqueue, stuck-active timeout), deterministic PII-free metadata + public endpoint, user + admin API (owner-scoped reads, admin retry/sweep), simulated minter (default) + TON testnet minter (standard deploy_item/transfer opcodes via the repo's `ton` fork; REQUIRES USER CONFIGURATION — minter wallet mnemonic + testnet collection), portfolio web UI (card badge + detail block + collectible disclaimer). 48 new API tests + 4 web tests; API 567/567, web 410/410. Docs: `docs/research/NFT.md`. Nothing committed.
- **2026-08-23** — Program file created (mirrored from the site-repo master plan; income model corrected to monthly accrual + 1%-fee/4-weekly-installment withdrawals). No code changes yet.
- **2026-08-24** — **Phase A complete (A0–A8).**
  - A0: baseline `npm run check` green; Docker unavailable → DB runtime checks skipped/flagged.
  - A1: "DigiHouse" → "FractionalLuxe" across all 12 locales + layout metadata, global-error, header titles, onboarding slides, share sheet, README title. Remaining grep hits are technical identifiers only (`digihouse-settings`, `digihouse_api_token`, traceId prefix).
  - A2: `fl-mark.svg`/`fl-logo.svg` imported to `public/images/`; legacy `public/seo/icon.svg` replaced with the FL mark; webmanifest + TonConnect manifest renamed; primary tokens + hardcoded accents aligned azure `#229ED9` / ink `#17212B` (`#3390ec` retained only in Telegram themeParams mirrors/comments). No raster logo usages existed to replace — wordmarks are text.
  - A3: seed now sources the 24-property manifest (`manifest-data.ts`, dry-run via `db:seed:dryrun`); idempotent upsert + legacy-row prune; contract unit tests added. Runtime DB verify BLOCKED (no Docker).
  - A4: copy aligned to monthly accrual in all 12 languages (+ new `withdrawalTerms` key); withdrawal terms line on earnings card and withdrawal-request sheet; weekly payout code left as-is (documented).
  - A5: unauthenticated `GET /public/properties[/:id]` (dollars, site convention), memory token-bucket rate limit, `PUBLIC_CORS_ORIGINS` env, smoke tests.
  - A6: `POST /public/waitlist` — validated, idempotent (`waitlist` table, migration `0027_waitlist.sql`, hand-written after drizzle-kit produced a bogus full-schema diff), tests green.
  - A7: KYC gap documented in README "Open items".
  - A8: phase gate green — see checklist above.
- **2026-09-03** — **R2 Canonical Estate Data Layer (product-owner approved; additive only — no UI, no manifest/seed/fixture edits, no settlement changes).** New `src/types/estate-canonical.ts` (R2 provenance OBSERVED/ESTIMATED/DERIVED/CONFLICTED/UNKNOWN + canonical record + reconciliation row types; barrel-wired), `src/lib/economics/estates/canonical-24.ts` (24 records keyed by existing runtime `prop-*` IDs joined to explicit Rental Escapes Listing IDs/URLs #1–#24: 128862/126855/108924/123861/125643/130393/130901/131293/128529/123320/109098/127825/122422/129548/122903/126870/130397/127483/108856/108860/106441/129549/123919/122113; observed identity vs research enrichment vs FractionalLuxe layers separated; Grand 2 BDM valuation $8M ESTIMATED/MODELED in approved ~$8–10M band, legacy $82M kept CONFLICTED, research $12–15M kept ESTIMATED; occupancy+income UNKNOWN for all 24; ANI land conflict preserved CONFLICTED; rate types 5 RANGE/2 STARTING_FROM/1 DYNAMIC/16 APPROXIMATE), `canonical-24.test.ts` (19 R2 audit tests). Competing registries preserved untouched (web `prop-*` runtime truth, API manifest-DB truth, econ-model truth). Verify: new tests 19/19 ✅, full web suite 753/753 (94 files) ✅, `npm run check` (lint+typecheck+build) green ✅. Nothing committed. Next: R2 Audit review → E Estate Detail IA per product-owner direction.
- **2026-09-03 (2)** — **Slice E: Estate Detail IA (rebuild; additive, no settlement/UI-system changes).** New view-model `src/lib/economics/estate-detail-view-model.ts` (sole engine touchpoint: canonical estate resolve — Grand 2 BDM only, others null/Unknown; baseline + occupancy envelope; share overview from listing facts + order-book asks; CTA kinds) + `src/hooks/useEstateDetailViewModel.ts` (hooks-boundary wrapper). New sections B–F: `EstateEconomicsSection` (scenario pills Base/Lower/Upper, nightly/ADR/occupancy/gross + provenance), `EstateCostBreakdown` (6 lines, progressive disclosure, green-tax pending), `EstateProfitAllocation` (40/60/net + 18%/gross bases; owner/operator pending while net unknown), `EstateInvestmentPanel` (primary/reference/secondary distinct, plans-unconfigured note), `ProvenanceTag`; `EstateTabPanel` extracted (PropertyDetail 308 lines, all files <350). Hero: canonical value row (Grand $8M Estimated; hidden otherwise) + market-driven CTA (legacy-equivalent outcomes). i18n: 43 keys en + EN-mirrored to 11 locales (translators follow-up). RentalStoryBlock/charts/funds/resale/buy/sell sheets untouched. Verify: unit 793/793 (97 files; +40 Slice E), `npm run check` green, e2e property-detail 4/4 incl. 480×840 overflow. Nothing committed. Next: F Marketplace per direction.
- **2026-09-03 (3)** — **Valuation decision (pre-F; additive data + wiring, no formula changes).** Research centrals approved as ESTIMATED/MODELED: 23 canonical rows now carry central-midpoint valuations (La Dolce Vita $32M point, HIGH confidence) with per-row source strings; ranges preserved verbatim in research context; Grand 2 BDM keeps $8M (band intact); no $82M anywhere; nothing labeled observed/exact. View-model exposes canonical `valuation` for all 24 (share reference now calculated across the catalog); hero/metrics/investment consume it (legacy mock never revived). Self-caught: bulk-insert script duplicated its anchor line in 23 rows (TS1117) — repaired mechanically, counts verified 24/24, typecheck clean. Verify: unit 797/797, `npm run check` green, e2e 8/8 (estate-economics + property-detail). Nothing committed. Next: Slice F per direction.
- **2026-09-03 (4)** — **Slice E Final Design/UX/Production QA (no model changes).** Raw-key allegation disproven: static audit 57/57 keys present 12/12 + live 3-estate scan zero leaks. Grounded 43-key translations for 11 locales (fa/ar verified live in RTL). Fixed: operator basis "13%"→exact "12.5%" (pct() rounds ≥10%); ownership/share "0.0%"→exact "1 / N"; $8M-vs-$820k hero/metrics clash already resolved pre-QA. Consolidated 3 repetitive unavailable blocks into 1 for unconfigured estates. Screenshots reviewed (EN/RTL/360px). Known gaps: unavailable-vocabulary ("Data pending") hardcoded-English app-wide (pre-existing convention); fundamentals legacy triple + RentalStoryBlock legacy narrative await Slice I reconciliation. Verify: unit 797/797, `npm run check` green, e2e 9/9 (economics+rtl+detail). Nothing committed. Next: Slice F per direction.
- **2026-09-03 (5)** — **Global ProvenanceInfo pattern (design-system change, presentation only).** New `src/components/common/ProvenanceInfo.tsx` (self-contained ⓘ trigger + Sheet with plain-language explanation; FeeInfoButton/Sheet primitives; haptics; aria-label/haspopup/dialog; 28px tap target) + 11 i18n keys ×12 locales (common namespace). Replaced all 20 ProvenanceTag chips across 5 Slice E files; deleted ProvenanceTag.tsx (zero references remain). App-wide audit: no other surface used repeated provenance labels. Data/engines untouched. Verify: unit 803/803 (98 files, incl. 6 component tests), `npm run check` green, e2e 9/9 (incl. live tap-to-explain + Esc dismiss + fa RTL + overflow). Screenshots reviewed (surface clean, sheet native). Nothing committed. Next: Slice F per direction.
- **2026-09-03 (7)** — **Design System Audit (Beautiful UI reference; no visual-identity change, no logic change).** Audited primitives + beautifului.dev (AI-dashboard set; mostly no legitimate TMA use case — documented per-pattern verdicts). Implemented 2 justified consolidations: shared `Disclosure` primitive (common/) migrating the cost-breakdown + resale price-history header expanders (row-embedded expanders + dropdowns intentionally unchanged — different shapes); shared `TabPanelSkeleton` (common/Skeleton) replacing 3 divergent copies (PropertyDetail local, EstateTabPanel inline, ResaleBlock fallback). ConfirmActionSheet/Row+StatusPill/analytics/cards/tabs/search/tables all reviewed → NO-CHANGE with reasons. Verify: unit 807/807 (99 files, incl. Disclosure suite), `npm run check` green, e2e 9/9. Nothing committed. Next: Slice F per direction.
- **2026-09-03 (6)** — **ProvenanceInfo hydration fix (button-in-button).** Chrome reported `<button>` descendant of `<button>`: the ⓘ trigger sat inside the costs-toggle expander button. Trigger reworked to the repo's FeeInfoButton pattern (`span role="button"` + keyboard handling) — valid nesting, same a11y contract. Regression tests: no `button button` descendant + Enter-to-open. Verify: unit 805/805, `npm run check` green, e2e 8/8. Nothing committed.
- **2026-09-03 (8)** — **Slice E Final Design Polish (no logic/model changes).** Full-page review (EN/fa/360px): above-fold hierarchy, 2×2 KPIs (kept — superior scanability), tab separation, funding rhythm, pills/costs/allocation/investment rhythm all approved as-is (existing geometry preserved). One genuine fix: scenario pills 36px→44px touch targets. No new components; no Beautiful UI imports; legacy narrative/charts/fundamentals untouched (Slice I). Verify: unit 807/807, `npm run check` green, e2e 9/9. Nothing committed. Next: Slice F per direction.
- **2026-09-03 (9)** — **Spacing root-cause fix (systemic, no model/logic changes).** Zoomed screenshots proved Slice E two-line cells bypassed the design system's rhythm primitives (zero-gap hand stacks). Fix: adopted shared `LabelStack` (6px title→hint, relaxed leading, bottom air, RTL-logical padding) in all 9 cost/allocation rows + `py-2` row air (EarningsEntryRow precedent) + `MutedText meta` for yield-projection labels. No one-off margins; Row/Block/section systems untouched. Before/after screenshots verified. Verify: unit 807/807, `npm run check` green, e2e 9/9 (incl. fa RTL). Nothing committed. Next: Slice F per direction.
- **2026-09-03 (10)** — **Slice E 24-property data reconciliation (no redesign, no logic changes).** Audited every visible Estate Detail field vs research dataset + canonical layer. Fixed: About description/size now approved research copy (fixture city prose + mock m² gone); year/type/lease/rental-status → labeled pending rows (labels kept); fundamentals section removed (legacy $820k/rent/yield had no source; value already canonical elsewhere; Slice I owns income); hero mock trust line removed; Trust tenant/lease chips → pending (payments chip keeps its simulated disclosure); nightly display now canonical dataset strings (fixes truncation). Kept: yield/income/lock/orderbook/position mechanics + labeled projections (current business rules, Slice I). Verify: unit 810/810, `npm run check` green, e2e 9/9 (about-sheet asserts live). Nothing committed. Next: Slice F per direction.
- **2026-09-04** — **Slice G: Investment / Buy Flow (UI-layer only; no settlement/engine/fee-model changes).** Existing Estate → qty → summary → confirm → success flow (primary) + limit-buy escrow flow (secondary) audited against the 14-section brief and hardened: new shared pure `src/lib/buy-quote.ts` (`previewBuyQuote`, integer cents) now backs BOTH the sheet total and the MainButton confirm label (previously ex-fees `$1,250` vs sheet `$1,281.25`); summary gained Location + Ownership (`N shares · P%`) rows and an Assumptions Disclosure (monthly-rate basis, lock-to-earn, weekly −1pp rule, no-plans-configured note — all existing copy/keys, zero invented inputs); qty step gained the lock-to-earn footnote; all buy copy (summary/success/MainButton, 21 keys) moved to `property.*` i18n en + EN-mirrored 11 locales. Zero plans exist in product (engine-only, no presets) → no plan UI invented; honest `invPlansNote` retained. Edge states pinned by tests: USDT-unavailable fallback, ok:false + wallet-reject failures, disconnected Connect-wallet MainButton, BackButton summary→qty cancel, invalid-qty message, LimitBuy unknown-book fallback; hook mutation chain (prepare→send→confirm→settle, phases) now tested. E2E `buy-flow.spec.ts` (5 tests: primary entry/gate/cancel, calculator entry, secondary place-order + toast, fa RTL) — MainButton-driven confirm/purchase stays unit-covered (no MainButton outside Telegram). Verify: unit 879/879 (106 files), `npm run check` exit 0, e2e buy-flow 5/5 + property-detail/marketplace/economics/rtl/home/smoke 24/24 green (income + portfolio e2e fail identically with and without this change — mock-mode data expectations, no API/dev-token in this env; unrelated). Screenshots slice-g/ reviewed (EN/RTL). Nothing committed. Next: Slice H per direction (NOT started).
- **2026-09-04 (2)** — **Slice H: Sell / Secondary Market Flow (UI-layer only; no settlement/matching/fee-model changes).** Existing instant buy-back (funding, −7% flat) + limit-sell order path audited against the 17-section brief and hardened: new shared pure `src/lib/sell-quote.ts` (`previewSellQuote` over the Slice C Share Model — `secondarySaleTotalUsd`/`gainLossVsAcquisition`/`remainingOwnershipAfterSale` — plus the canonical `sell_secondary` tier preview; integer cents; gain = gross − cost BEFORE fees, net = gross − fee shown separately; unknown basis/fee stay unknown) backs the whole custom flow; `resolveSellListingStatus` derives Queued/Active/PendingLiquidity/NoLiquidity/PartiallyFilled/Filled/Cancelled from the existing order + live book (book unknown → generic pending, never "no buyer"; Active ≠ Sold). SellSheet split to the ownership-guard shape (orchestrator 322 + `sell/` panes: Instant 205 / Custom 224 / QtyStepper 71 / QuoteSummary 101 / ListingStatus 188; UI only via hooks, never mock/TON directly). Custom flow is now form → review (`Review your listing`: estate/shares/ownership-sold/price + gross/cost/gain-loss/fee/net/remaining + queued-vs-live honesty note) → `List for Sale` CTA (`Queue sell order` while funding) → listed state with liquidity pill + market context (best bid/last) + backend-confirmed cancel (armed confirm → Cancelled on mutation success). Instant review gained cost-basis/gain-loss/remaining rows; instant math untouched. Position header (owned shares · ownership %, avg cost) + free-share guard + honest `Nothing to sell` empty state; oversell/zero-price/invalid blocked with messages; 27 sell keys en + EN-mirrored 11 locales (translators follow-up). Callers pass real `ownedShares` + live `orderBook` (PositionCard/PropertyDetail/page/YieldLockSection). E2E `sell-flow.spec.ts` (4 tests: gain/loss/break-even + remaining + no raw keys + no guaranteed/instant promises; review → Active → cancel → Cancelled; empty-position honesty; fa RTL) + `screenshots/slice-h/` reviewed (EN/RTL, 480×840, no overflow). Verify: unit 910/910 (107 files; +23 sell-quote, +9 SellSheet), `npm run check` exit 0 (lint 0 errors + 7 pre-existing warnings), e2e sell-flow 4/4 + buy-flow/property-detail/marketplace/home/smoke 19/19 green; estate-economics 8/8 + estate-rtl 2/2 green; income e2e 1 failure is pre-existing (stale "Bayside Marina Penthouse" title vs reconciled "Syrene" seed — no Slice H file touches income/titles; fails identically without this change). Screenshots reviewed. Nothing committed. Next: Slice I per direction (NOT started).
- **2026-09-04 (3)** — **Bottom-CTA spacing fix (root cause, 1 word + prior 2 one-class gaps).** The final Acquire+Sell sticky bar sat directly under/overlapping the last content card because `.page-enter`'s `dh-page-in … both` fill kept a computed `matrix()` transform forever, making the wrapper the containing block for all `position:fixed` descendants (un-floated the sticky CTA + compact top bar in every Chromium webview, incl. Telegram Android). Fix: fill `both` → `backwards` in `src/app/globals.css` (pixel-identical enter frames; verified the wrapper computes `transform: none` and the bar anchors to the viewport again). Kept last round's `pt-4`/`pt-2` on the PositionCard/YieldLockSection side-by-side rows (24px intra-section gaps; files otherwise untouched this round). Measured @480×840 true page bottom: secondary/ownership last-card → button-top **36px**, primary/estate **120px** (pre-existing #08 clearance, untouched). Verify: `npm run check` exit 0, unit 910/910, e2e property-detail/buy-flow/smoke 13/13. Nothing committed.
- **2026-09-04 (4)** — **Slice H final UX polish H1–H8 (UI-layer only; no matching/settlement/fee/ownership logic touched).** H1: sheet opens on Custom price when instant is unavailable (initial mode from `listing.status`, instant preserved on funding). H2: custom form reordered to position → available → stepper → market context → price → outcome → Review CTA (one-block move, review untouched). H3/H8: listed headings now status-driven (`Your listing is active` / `Sell order queued` / `Listing cancelled` / Sold) — `sellOrderPlacedToast` key retired from UI but kept in locales. H4: `Available to sell: N shares` + real locked count (`Locked: N · not sellable`, derived owned−free). H5: `findFeeTier` extracted as the single canonical lookup (`previewFeeUsd` refactored onto it, behavior identical); quote carries `feeRateBps` → `Market fee (0.90%)` + `Charged only when your shares sell.` caption (model-verified: sell fee settles on fill notional; instant 7% path untouched). H6: full fa set from established vocabulary (avgCostPerShare/notSellable/nShares/orderQueued/escrow wording precedents; سربه‌سر is the standard break-even term, no in-repo precedent — flagged); other 10 locales EN-mirrored, parity 404/404. H7: listed remaining reuses the same quote object (`101 · 4.0%`). Tests test-first throughout (watched RED each cycle): unit **916/916 (108 files)**, `npm run check` exit 0. E2E: sell-flow 4/4 + buy-flow/property-detail/marketplace 13/13 + estate-rtl 2/2 + estate-economics 8/8 green (one estate-economics bound `<60` updated to `<200` — it encoded the pre-fix glued-bar layout; overlap guards kept). Browser QA @480×840 LTR + fa RTL with `screenshots/slice-h-polish/` (entry/gain/loss/review/active/cancelled + fa form/review/listed; quote→CTA 16px rhythm; no overflow/clipping). Nothing committed. Next: Slice I per direction (NOT started).
- **2026-09-04 (5)** — **Slice H follow-up: custom price input + market guidance (UI-layer only; no matching/settlement/fee/ownership logic touched).** Bug: `onChange(Math.max(1, Number(...) || 1))` made clearing impossible (`125→""` stuck at `1`). Fix: raw-text `priceText` state (`""` legal while editing) with validation/quote/submit deriving from the parsed value separately; min-$1 preserved as validation, never coercion. Guidance: `selectMarketReference` (best offer → last price → silent) + `priceGuidance` (±5% integer band) in `sell-quote.ts`; calm note directly under the price field, informational only (quote/order untouched). i18n: 2 keys ×12 (fa in natural Persian from the brief's wording). Tests test-first (RED observed incl. exact `expected '' got '1'`): unit **932/932 (108 files)**, `npm run check` exit 0. Browser QA @480×840: clear→empty→retype 100/150, below/above/normal guidance with the live $245.98 best-offer reference, no stale warnings, fa RTL fully localized with no overflow. Screenshots `screenshots/slice-h-price/`. Nothing committed.
- **2026-09-04 (7)** — **Slice I Observatory: Investor Income Observatory (presentation + pure chart/view models; no business/accounting/fee/settlement logic touched).** Reworked /earnings around five experiences: Pulse hero kept (already matched the paid-first spec); IncomeJourneyChart replaces the static 12-week chart (paid/projected tappable columns ≥44px, cumulative received line, 12W/6M/1Y/ALL ranges that clamp honestly to history, detail panel with date/paid/projected/count/estate drill-down links/current accrued, full keyboard + aria-live support); DistributionStatus rebuilt as a 5-node vertical journey (Accrued→Eligible→Requested→Scheduled→Paid out) with a next-distribution callout; IncomeByEstate gained a paid-only segmented composition (total + legend + deterministic top-contributor insight) plus per-row rank bars; OperatingIncomeExplainer restyled as a connected flow (same copy). New `src/lib/income-chart-model.ts` (typed points, ranges, cumulative, composition). i18n: 11 observatory keys en + EN-mirrored ×10 + real fa. Tests test-first: unit **985/985 (114 files)**; `npm run check` exit 0; E2E **30/30** incl. rewritten journey assertions (old static-chart testids retired with documented reason) and one estate-economics bound `<60`→`<200` (it encoded the pre-fix glued-bar layout; overlap guards kept). Browser QA @480×840 LTR + fa RTL (`screenshots/slice-i-obs/`): exploration rewards understanding, no English leaks, no overflow. Nothing committed. Next: launch sign-off per user direction.
- **2026-09-04 (6)** — **Slice I: Profit / Income Experience (UI-layer + pure view-model; no matching/settlement/fee/ownership logic touched).** All six states traced to real backing data (pending sums, lock-accrual engine, free-share/withdrawable rules, withdrawal requested+installment ledger, paid entries + paid installments). New pure `src/lib/income-view-model.ts` (IncomeState known/pending/none; distribution pipeline with rejected excluded; per-estate projected/accrued/paid; secondary gains via Slice H math on live sell orders). New sections on /earnings reusing Block/Row/Skeleton/Disclosure/StatusPill: `DistributionStatus` (eligible/requested/scheduled/paid-out), `OtherReturns` (plan → honest not-configured; appreciation → Pending; live listings with proposed gain, never income), `OperatingIncomeExplainer` (collapsed 5-step chain + Estate-tab pointer, no second engine); `IncomeByEstate` extended with ownership + projected/accrued lines (legacy path byte-identical when position data absent). Per-estate economics stay on the Estate tab (linked from every row). i18n: 25 earnings keys en + EN-mirrored 10 locales + full fa from established vocabulary (زمان‌بندی‌شده/سربه‌سر flagged for translators). Tests test-first: unit **962/962 (112 files)**, `npm run check` exit 0. E2E 29/29 incl. rewritten income.spec (stale pre-reconciliation title fixed with documented reason + new-state assertions). Browser QA @480×840 LTR + fa RTL (`screenshots/slice-i/`): no fake $0, hero paid-only excludes the $30 listing gain, no English leaks. Nothing committed. Next: launch sign-off per user direction.
- **2026-09-04 (8)** — **Slice J: Conversion & Premium Polish (UI-layer only; no engines, no formulas, no new numbers, no settlement/fee/ownership changes).** Prioritized by conversion friction (worst first): Estate Detail hero → Marketplace cards → Buy pre-confirm → Sell quote → Income readability. Marketplace `PropertyCard`: entry-price hero (15px bold vs 13px metrics), explicit `View estate ›` footer affordance (new `estates.viewEstate` key; whole-card nav e2e still green), FeeInfoButton removed from photography (fee info lives in Buy summary), name `text-balance`. Detail `PropertyHero`: money-chain primer (`property.heroMoneyChain` — "Nightly stays → costs → owner share", zero numbers, engines A/B stay the source) + genuine-supply caption under CTA (primary + remaining>0 only, reuses `fundedCaption` + listing facts, same rounding as FundingPanel) + `text-balance`/`break-words` hardening. `PropertyMetricsGrid`: `min-w-0` + `truncate` for large currency. Buy (`BuySummaryStep`/`BuyQtyStep`): total row hero (15px bold + hairline), projected yield moved off Paid-green to foreground (Projected≠Paid now unmistakable; labels already carried Projected). Sell `SellQuoteSummary`: net-proceeds hero (15px bold foreground — a proposed total, never paid income; gain-vs-cost keeps honest green/red). Income (readability only, Slice I separations intact): by-estate Telegram hairlines + airier rows, journey legend `flex-wrap` (RTL/long strings), hero/distribution `break-words`/`min-w-0`. i18n: 2 keys en + EN-mirrored ×10 + real fa (مشاهده ملک + Persian chain line; translators follow-up for the 10). Audit: no engine imports added (only the Slice F view-model type), no numeric literals beyond existing listing facts. Verify: unit **985/985 (114 files)**, typecheck + eslint + `npm run build` green; E2E **29/29** (rtl 2 + marketplace 4 + property-detail 4 incl. 480×840 overflow + economics 8 incl. all-24 sweep + buy/sell 9 + income 2); rendered DOM measured 0px overflow both surfaces, hero chain + "92% funded · 200 shares remaining" live-verified, zero page errors; screenshots `screenshots/slice-j/` (marketplace/detail-hero/detail-estate-tab @480×840). Known limitation (out of scope, no formula change): card projected income ($5.37) vs detail metric ($5.75) come from different legacy mock paths — unify on canonical engine output when operating reports exist (product decision). Nothing committed. Next: launch sign-off per user direction.

- **2026-09-09** � **Financial Model V1 build (PM-approved; additive engine + types + inputs + tests only � no UI, no settlement/TON, no portfolio changes, no legacy-economics edits).** New src/types/financial-model-v1.ts (ANR input with currency+scope+provenance+method, single ESTIMATED valuation, owner-tax rate|unknown union, excluded guest-paid charges, scenario/average/per-share shapes; per-share labeled Projected), src/lib/economics/financial-model-v1.ts (ONE canonical engine: gross=ANR x 220/273/328, minus 5pct agency minus 7.5pct operator minus 1.5pct-of-value reserve = pre-tax, minus owner tax = net, 75pct owner / remainder operator so allocation reconciles; average = same chain at mean gross; integer cents half-up; USD reserve vs EUR gross yields downstream UNKNOWN, never FX-converted; tax/projection/reserve disclaimers), src/lib/economics/estates/financial-model-v1-inputs.ts (24 inputs derived verbatim from ESTATE-24-DATA.json + CANONICAL_RECONCILIATION; only hand-authored layer = locked owner-tax table: T&C 0pct, BVI 0pct, St Barth UNKNOWN, Nevada UNKNOWN, California UNKNOWN, Maldives ~10pct withholding, rest UNKNOWN; dataset drift throws), src/lib/economics/__tests__/financial-model-v1.test.ts (35 tests: all 18 required validations + Grand exact-math pins + reconciliation identities). Result states: 9 full-chain (T&C/BVI/Maldives), 10 pre-tax-only (unknown owner tax), 5 gross-side-only (EUR mixed-currency). Grand average: gross 26608678.42 USD, per-share annual 195.43 USD / monthly 16.29 USD. Verify: new tests 35/35, full web suite 1085/1085 (120 files), typecheck clean, lint 0 errors (7 pre-existing warnings), build green. Design/UI QA: zero UI files touched (4 new + 1-line barrel); V1 outputs verified formatting-compatible via existing usd/usdCompact helpers; noted pre-existing pct() sub-10pct quirk (5.0pct) for the future Detail screen. Nothing committed.
- **2026-09-10** - **PROMPT 05 funnel refinement, V1-aligned (targeted refinement, no IA redesign; no formulas/settlement/matching/TON/buy-sell changes).** Estate=Desire / Income=Conviction / Ownership=Decision; Detail untouched. V1 wired as sole authority via estate-detail-view-model 1 (mechanical integration only). New: EstateV1Thesis (ANR, modeled 220/273/328 revenue range, projected per-share, scenario-not-occupancy note), IncomeV1Story (rental basis with ANR method, 4 scenario pills incl. average, 5pct/7.5pct/1.5pct costs, owner tax with assumption disclaimer, net, owner 75pct, per-share Projected, excluded guest-paid charges, honest Accrued-vs-Paid position block), OwnershipV1Panel (nominal , V1 totals, 1/N, position-projected only when trading price equals V1 nominal, consequences, Buy entry into existing Slice G/H, holders honestly pending). Retired from tabs (files kept, tested in isolation): Slice A economics/costs/allocation, FundingPanel, PrimaryPerformanceCharts, simulated IncomeAnalytics, yield-rate IncomeCalculator, simulated HolderAnalytics. Data: Grand active valuation exactly  single (retired - band display; .6M- stays research evidence; .5M research-only;  quarantined), Grand fixture -> + value -> + rent chain intact, canonical fixture-price sync, Growth  no pct preserved, EstateInvestmentPanel/Hero-fraction/Metrics-monthly on V1 (/80k/.29). i18n: 44 property keys en + EN-mirrored x11. Assumption stated: fixture supply reads as the initial  offering tranche of V1 shares (same price/class); full tranche-vs-total mechanics remain a Slice G/H product decision. Verify: unit 1086/1086 (120 files), 
pm run check green (lint 0 errors + 7 pre-existing warnings), e2e estate-economics 8/8 + estate-rtl 2/2 @480x840 LTR/RTL incl. all-24 sweep (V1 architecture, zero overflow, no raw keys), screenshots slice-e-qa regenerated (incl. truncation fix: thesis revenue wraps). Remaining: BuySheet/SellSheet still execute at fixture trading params (Slice G/H frozen, out of scope); non- fixture prices beyond Grand await ownership-accounting decision; legacy tab components remain on disk unused. Nothing committed. Next: user review -> Prompt 06 (STOP).
- **2026-09-11** — **Product Decision Lock & source-of-truth reconciliation (labels/docs only; no payment/settlement/TON/math changes, no UI redesign, no canonical-data replacement).** New `docs/product/rebuild/PRODUCT-DECISION-LOCK.md`: Tier 1 Rental Escapes identity / Tier 2 ESTATE-24-DATA.json+V1 economics / Tier 3 research-evidence / Tier 4 legacy-mock hierarchy; decisions A–F enforcement points; 11-item reconciliation; 8 PO-approval items (trading-tape migration, V1-vs-fixture supply, lock weekly-option model, $80→$100 confirm, ESTATE-24-DATA.md companion, secondary mechanics, 1% legal meaning + legal wrapper, open §19 economics). Fixed live violations: onboarding slide 2 weekly→monthly + slide 1 $80→$100 (`onboarding-slides.ts` + test); buy-sheet `estWeeklyYield`→`estMonthlyYield` monthly figure (same A4 weekly×52/12 number as cards/lock preview; `BuyQtyStep`/`BuySummaryStep` + `BuyFlow.test`); `ownershipV1NominalNote`→"$100 base price per share at the primary offering … secondary separate" ×12; `property-yield.ts` comments distinguish monthly communication vs installment schedule and lock −1pp vs withdrawal 1% (math untouched); entry claims $80→$100 (`own.subtitle`, portfolio `emptyMessage` ×12). Repaired cp1256-mojibake in 5 EN-mirrored V1 keys ×11 locales (byte-verified zero residue, JSON re-validated). Deliberately untouched (documented): lock payout-period weekly option, Sunday settlement cadence strings, fixture $85–$150 trading tape, V1-vs-fixture dual supply. Findings: ESTATE-24-DATA.md has no file (JSON canonical); no NFT legal-title claims; no partnership claims; demo disclosure intact. Nothing committed. Next: validation results → final report per user direction (STOP).
- **2026-09-11 (2)** — **Final PO Decisions 1–8 applied (labels-boundary-economics; no valuation/identity changes, no redesign, no history rewrite, no commit).** New `canonical-offering.ts` (V1 valuation÷$100, $100 base — Grand 80,000) + `mock/canonical-listing.ts` boundary (Tier-1 identity, V1 supply/price, demo-ledger sold/remaining; seed never mutated); `MockMarketplaceRepo` serves canonical listings; `confirmBuy` values/ratios at $100/canonical; instant sell at $100−7% (fixture mutation removed); locks monthly-only seed + weekly→monthly coerce (history preserved); NFT/CSV names canonical; seeds re-based ($100 cost/value, canonical ratios); cards/featured/buy projections full-rate monthly (−1pp reuse removed); `LockSheet` monthly-only + `legacyTag` for preserved weekly records; `WEEKLY_RATE_PENALTY_PP`→`LEGACY_*` vs `WITHDRAWAL_FEE_BPS` separation; `bannerCoOwned` removed (non-primary → Resale market); trust sold-row gated on ledger; `demoOrderBookNote` on MarketSummary; `estates/emptyMessage` weekly-cadence promise dropped ×12; dead weekly-framed keys removed; new `LockSheet`/`canonical-offering`/`canonical-listing` tests; generated `ESTATE-24-DATA.md` via new script; lock-doc §6 + this log. Stale specs aligned with documented reason (sell/income $100-base values; property-detail V1 panel; buy-flow ownership entry; portfolio onboarding pattern; ownership-touch retired-holder-charts spec deleted). Verify: unit 1096/1096 (123 files), lint 0 errors, typecheck clean, build green, e2e full-suite green @480×840 (incl. fa RTL + fresh QA screenshots `screenshots/decision-lock/`). Nothing committed. Next: user review (STOP).
- **2026-09-13** — **Estate Page Structure source of truth + shared section components (user-directed; structure-only — NO villa page changed, no data wired, no i18n keys added, no engine/settlement/buy-sell/lock changes).** Created `docs/design/ESTATE-PAGE-STRUCTURE.md` as the single source of truth for the Estate detail IA refactor: fixed L0 top section (gallery / name / location / price + 1-of-N fraction / funding bar / estate value / Buy + "Withdraw anytime — 1% fee, paid in 4 weekly installments" helper) and fixed L1 4-stat section (Monthly Income · Proj./Year · Avg. Nightly Rate · Est. Growth — ANR never ADR, Projected never Paid), then the 4 tabs reorganized section-by-section — Estate: why-this-estate → key specs → amenities → location; Income: rental basis → 4-scenario revenue cards (per-scenario nights/gross/per-share yr+mo) → expandable modeled costs → not-deducted guest-paid charges → net-economics summary; Ownership: valuation 2×2 → growth potential → exit & liquidity → position preview → risk disclosures; Details: operator → legal & structure → documents → distribution & tax → historical performance (only if real data). Every section is mapped to its existing data source (Financial Model V1, ESTATE-24-DATA.json via `estate-24-data.ts`, canonical/listing layers) with a locked preserve list (P1–P6: buy/sell buttons, Rental Escapes secondary link, buy/sell sheets, lock/unlock, sticky top bar + bottom CTA, all flows/logic untouched) and the locked execution order: structure → data unification → villa-by-villa refactor behind §10 gates. 11 open data decisions (D1–D11) recorded instead of invented — incl. the Base-vs-average per-share conflict (user rule: Base must equal Monthly Income/Proj.-Year; V1 `perShare` is average-based) and occupancy (template asks for it; data says UNKNOWN, locked V1 basis is modeled nights). Built 3 presentational shared components — unwired (no villa page imports them), props-in/callbacks-out, strings via props (no i18n changes), zero engine imports, all <350 lines: `src/components/property/ScenarioCards.tsx` (controlled scenario card set, one expanded card, Base default via caller state, aria-expanded/controls), `ExpandableCostRows.tsx` (independent per-row expansion with basis line + detail + meta rows, FactRow visual rules), `IconPointsGrid.tsx` (read-only icon+label grid, 2/3 columns, null when empty). Tests: 3 new colocated suites, 16 tests (controlled selection, independent expansion, a11y wiring, column variants, empty state). Verify: new tests 16/16 ✅, full web suite 1092/1092 (131 files) ✅, `npm run check` exit 0 ✅. Nothing committed. Next: user review of structure + components (STOP).
- **2026-09-13 (2)** — **D1–D11 locked decisions applied: additive Estate data modules + V1 owner-tax table extension (user-directed per the locked-decisions hand-off; data layer only — NO villa page UI changed, no new dependencies, no payment/settlement/TON/secondary-market changes; D9 secondary-market items explicitly REMOVED from this wave).** New leaf type layer `src/types/estate-page-data.ts` + four additive data modules under `src/lib/economics/estates/`, all joined to the frozen dataset via the existing `getEstate24ByRuntimeId`/Estate24 id (1–24) join (verified 1:1 against ESTATE-24-DATA.json before implementation): (1) `operator-24.ts` (D1 CONFIRMED) — Rental Escapes company facts + 12 Villa Specialists mirrored **mechanically/verbatim** from `d1-rental-escapes-specialists.json` via a generation script (12/12 photo paths verified on disk under `public/operators/specialists/`; zero CDN URLs; the only sanctioned edit is the documented cp1256 mojibake→em-dash repair, two sequences in Andrea Ducharme's bio) + deterministic `parseAssignTo("villas A-B")`→estateId assignment with overlap/gap guards; (2) `location-details-24.ts` (D7) — 24 verbatim rows: exact location text, island/resort, region, airport transfer with `status: CONFIRMED|APPROX` (11/13; isEstimate follows status exactly), optional highlight/note (incl. the Trajan "hotel suite product" legal flag); no map asset → text-only; (3) `legal-structure-24.ts` (D2/D3/D4) — Maldives dedicated SPV/head-lease text keyed to estateId 1 ONLY (isEstimate false) + generic SPV template (isEstimate true) for the 23; standard D3 insurance coverage with per-villa insurer/policyYear TBC (null); D4 valuation method with the locked generic phrase "Independent RICS-registered valuer" (no firm names — pinned by test), 2024-12-31 date flagged approx (roll to 2025-12-31 at launch); (4) `estate-page-constants.ts` (D5/D6/D8/D10/D11-disclosure) — 10 locked document titles (documentsAvailable=false → pending until real PDFs), 6 risk disclosures, honest D8 lines, D10 growth +3–5% p.a. (Knight Frank PIRI, isEstimate, illustrative), D11 investor-facing tax paragraph with the not-tax-advice disclaimer. **D11 engine change** (`financial-model-v1-inputs.ts`): owner-tax table extended from 13 to 24 explicit entries — St Barth 0% (resident basis; home-country not modeled), Nevada 0% state (federal not modeled), California 13.3% state-top (federal not separately modeled — combined often higher, flagged in note), and range-jurisdictions applied at MIDPOINT per the locked derivation rule: Sri Lanka 12% (10–14), DR 21% (15–27; CONFOTUR may reduce), Jamaica 25%, Mexico 27.5% (20–35), Thailand 17.5% (0–35), Italy 23.5% (21–26 cedolare), France 32.5% (20–45+), Austria 27.5% (0–55); every entry status ASSUMPTION + "Seek personal tax advice"; the 9 pre-existing PM §7 notes gained the same seek-advice line. Consequence (user-approved): 19 full-chain models / 5 EUR gross-side-only (no-FX rule intact) / 0 unknown-tax — per-share PROJECTED figures now exist for the 10 formerly pre-tax-only villas. Tests: 4 new suites (`operator-24` incl. JSON parity + on-disk photo check + no-mojibake, `location-details-24`, `legal-structure-24`, `estate-page-constants`) + D11 pins in `financial-model-v1.test.ts` (zero-rate count 8→11, no-unknown invariant, per-jurisdiction rates, 19/5 states) and updated presentation pins (`property-presentation.test.ts` 15→5 unknown, 9→19 computable; UI pending-fixture tests re-pointed from now-full-chain Trajan/Villa BDM to EUR Chalet Montana with documented reason) + `estate-economics.spec.ts` updated (STARTING_FROM nightly semantics kept on Trajan; honest-UNKNOWN moved to the EUR peer). Verify: unit **1114/1114 (135 files)** ✅, `npm run check` exit 0 (lint 0 errors + 6 pre-existing warnings) ✅, e2e estate-economics 8/8 + estate-rtl/property-detail/income/buy-flow/sell-flow/marketplace 22/22 + home/smoke/earnings-honesty/buyer-journey/portfolio 14 pass (1 pre-existing portfolio flake) ✅, order-lifecycle 3/3 standalone (parallel-load flake, pre-existing documented 2026-09-12) ✅. Nothing committed. Next: user review (STOP).
- **2026-09-13 (3)** - **VILLA 1 refactor: Grand 2 BDM (JOALI Being, re-128862) rebuilt to the locked Estate Page Structure (user-directed per structure s10; villa-gated step 1 of 24).** Fixed L0 hero (gallery/name/location/$100+fraction/funding bar/$8M value/Buy + withdrawal-terms note) UNTOUCHED; new fixed L1 4-stat section (PropertyMetricsGrid rebuilt): Monthly Income + Proj./Year = the presented BASE-scenario per-share figures (PO rule: Base must equal the scenario card - the presentation path switched off the retired average-based perShare; one canonical path shared by marketplace cards, detail and buy flow), Avg. Nightly Rate = V1 ANR (never ADR), Est. Growth = the locked D10 band (+3-5% p.a., Knight Frank PIRI, estimated); price/funded cells live in the hero only. Tab 1 ESTATE (s4): Why-this-estate (Estate24 description.short + highlights DERIVED verbatim from the record's own amenity/service strings + the D7 highlight - no authored copy), Key Specs (canonical facts via FactRow; year-built/rental-status/lease = honest pending rows), Amenities icon grid (verbatim dataset labels, cap 10), Location (D7: exact text, island/resort, region, transfer with CONFIRMED-observed / APPROX-estimated provenance; text-only - no map asset); protected Reserve CTA stays last; resale block preserved for secondary/sold-out (not rendered for villa 1); RentalStoryBlock/EstateV1Thesis/EstateInvestmentPanel retired from the tab (files kept). Tab 2 INCOME (s5): Rental Basis (ANR leads + method + modeled occupancy ~75% on the AVERAGE basis per PO decision 2026-09-13, derived nights/365 and labeled modeled - never observed - + honest historical-pending line), Scenario Revenue (shared ScenarioCards: 4 locked scenarios, Base expanded by default, per-card nights/gross/modeled-occupancy/per-share-yr+mo via the locked owner-profit/shares derivation), Modeled Costs (shared ExpandableCostRows: agency 5% / operator 7.5% / reserve 1.5%-of-value, each expandable, reserve carries the no-FX unknownReason; no invented other-cost line), Excluded charges (A_GUEST_PAID verbatim + green check + locked not-deducted note), Net Economics summary (net-distributable hero + locked 75% owner share + owner-side tax with honest unknown + per-share yr/mo for the SELECTED scenario), preserved position-income block. Tab 3 OWNERSHIP (s6): Valuation & Shares 2x2 ($8M / $100 reference / 80,000 shares / 1/80,000), Growth (D10 assumed band + research potential, estimated), Exit & Liquidity (locked copy only; D9 stays removed), Position Preview ('1 share today' + stepper; 12-month projection omitted per structure s6.4), Risk Disclosures (6 locked statements via Disclosure); PositionCard/OwnershipBanner/OwnerStayCard/YieldLockSection preserved unchanged (lock/sell flows e2e-verified). Tab 4 DETAILS (s7): preserved PropertyTrust, Operator (D1: Rental Escapes facts + Amanda Singer for villas 1-2, local photo), Legal & Structure (D2 Maldives SPV/head-lease text villa-1-only + D3 insurance TBC + D4 valuation date/method/valuer generic phrase), Documents (10 locked D5 titles, honest pending - no fake downloads; API document list unmounted, file kept), Distribution & Tax (monthly accrual + honest schedule line + D11 disclosure + Maldives supplement), Historical Performance (D8 honest disclosure only). i18n: ~74 new property.* keys EN + mirrored to 11 locales (12/12 parity verified) with real fa for headline labels; e2e raw-key checks moved to key-shape detection. Tests updated with documented reasons (presentation Base pins $16.25/$195.00 for Grand; consistency suite asserts a hero-only price surface; PropertyDetail/SliceE suites rewritten to the new section testids; page.test price surface retargeted; 5 e2e specs rewritten incl. the all-24 sweep on the new architecture). Verify: unit 1112/1112 (135 files) OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings OK; npm run check exit 0 OK; full e2e 46 passed / 6 skipped (money-path) / 0 failed at 480x840 incl. fa RTL + all-24 sweep + overflow audits OK; all touched files <=210 lines (ownership guard) OK. Preserved behaviors e2e-verified: buy/sell sheets, MainButton flow, lock/unlock sheet, sticky CTA, hero CTA states, reserve CTA external popup, order lifecycle. Nothing committed. Next: user review of villa 1 (STOP).
- **2026-09-13 (4)** - **VILLA 1 revision pass applied (user revision contract, 20 numbered sections; same locked scope - 4 fixed sections + 4 tabs only; preserve list untouched; nothing committed).** Expandable-row (TaskRows) contract applied everywhere appropriate; text walls removed; pending filler rows eliminated. Estate tab: Why-this-estate is now a highlighted card (softer surface) driven by a NEW PO-supplied editorial layer (estate-editorial.ts, villa 1 only: 1-line headline + max-2-line body + 4 highlights; villas without approved editorial fall back to their dataset description - PO copy is approved content, never invented); Key Specs dropped the year-built/rental-status/lease pending rows entirely (no data - no row) with a two-line Size (total + interior/pool); Amenities grid now 2-col mobile / 3-col sm+ (IconPointsGrid gained smColumns + truncateLabels props), first 10 with Show more toggle, full untruncated labels; Location card carries the big location title, Island/Region rows, the transfer as a COLLAPSED row (value = status + leading duration derived from the locked text, e.g. 'Confirmed - ~40 min'), and the PROTECTED Reserve CTA moved inside the card (same component/testid/URL and external-popup behavior - position changed only). Income tab: Rental Basis is now a metric PAIR (ANR big + 'Full-buyout mean (Rental Escapes) - not ADR' caption; modeled occupancy ~75% + 'From modeled nights (Average scenario)') with historical occupancy as a collapsed row (2 detail lines); Scenario cards renamed 'Projected revenue scenarios' with the fixed expanded order modeled nights -> whole-villa revenue -> per-share year/month (rendered in the accent color; the per-scenario occupancy row moved out - occupancy lives on the Rental Basis); Modeled costs rows now combine basis AND amount ('5% of gross - $1.6M') with the contract's detail bullets (ExpandableCostRows gained a bullets prop); not-deducted list renamed with the locked transparency footer; Net card rebuilt as 'Your net income' - distinct darker surface, hierarchy net-distributable + 75% owner share -> per-share/year (large) -> per-share/month (largest, accent) -> owner-side tax small with provenance info + 'Projected figures only' footer. Ownership tab: ownership-per-share cell gained the exact percentage (0.00125%); growth rebuilt as the big D10 metric + source caption with the research upper estimate as a collapsed row; exit card trimmed to the two locked rows + caption (market-status row removed); FIXED the position-preview Current value bug (shares x price double-scaled x100 - $10K error; now exactly sharePrice x qty); risks renamed 'Important risks'. Details tab: pending-only trust block REMOVED this wave (s15: nothing genuinely green exists); operator card horizontalized with clamped 3-line bio + Show more and derived company monogram; Legal & Structure rebuilt as three collapsed rows (SPV-Leasehold/insurance-full/valuation date+APPROX with method+valuer details; insurer/policy rows render only with real data); Documents show 5 + Show all (10); Distribution & Tax keeps the two rows and moves the D11 disclosure (+Maldives supplement) into a collapsed 'Owner-side tax summary' row; Historical unchanged. i18n: ~35 new/renamed keys + 3 title value updates, mirrored 12/12 parity with real fa. Tests/e2e updated with documented reasons (trust assertions removed, legal/documents/transfer expansion steps, reserve-CTA position now inside the location card, growth label casing shared with the card, preview title). Verify: unit 1111/1111 (135 files; -1 obsolete trust test) OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings (baseline) OK; npm run check exit 0 OK; full e2e 46 passed / 6 skipped / 0 failed incl. fa RTL + all-24 sweep OK; standalone property-detail 4/4 after one parallel-load flake (documented class); screenshots reviewed - the rendered page matches the revision contract. Next: user review of the revision pass (STOP).
- **2026-09-13 (5)** - **VILLA 1 directive wave 3: Yield section removed, new Earn tab, BorderBeam, specialist photo lightbox, red-mark fixes (user-directed; same locked scope - preserve list intact; nothing committed).** (1) YIELD REMOVED: YieldLockSection unmounted from Ownership and DELETED with its test (roots cleaned: no estate-page surface shows the yield rate anymore; the rate survives ONLY inside the protected lock product (LockSheet preview + fixtures - program-protected lock/settlement math untouched); owner Lock/Sell stay reachable - PositionCard (secondary, both buttons) and OwnershipBanner (primary) which GAINED a Sell button + SellSheet wiring (same protected flows, new location) - both moved to the new Earn tab. (2) EARN TAB: tabs now Overview - Income - Ownership - Earn - Details (PropertyTabId + strip + i18n tabEarn x12); new EarnTabPanel with ONE strong 'Your position' section per the contract: 'If you invest today' + subtitle, +/- share stepper, Investment row (= sharePrice x qty exactly - the $10K bug stays dead), two equal metrics (Monthly income scaled by qty with 'Projected from Base scenario'; Estimated value growth 12 months as a RANGE +$3-$5 scaled from the D10 3-5% band with 'illustrative' caption), illustrative After-12-months snapshot (income received approx + value-of-share range), mandatory 'Projected figures only - not guaranteed. Growth is illustrative (Knight Frank PIRI range).' footer, Buy CTA (earn-buy) into the existing flow. BorderBeam (user-approved dependency border-beam@1.3.0, official API only): size=md + colorVariant=ocean + strength=0.7 + theme=dark on the Earn main card; one-shot size=line beam on the Earn TAB CHIP at page load (~1.8s then active=false); jsdom matchMedia stub added to the test setup (border-beam needs it). (3) RED-MARK FIXES: Key specs Size renders exactly two lines from the structured specs (total; interior - pool) - the duplicated combined string is gone (sizeText no longer used there); Location title strips the resort name (appears once, as 'JOALI Being - 68 villas - wellbeing island' in the Resort row) with deterministic transforms on the locked D7 fields; Rental basis ANR label now 'Avg. nightly rate (full buyout)' with caption 'Mean of distinct full-buyout rates - not ADR'. (4) LIGHTBOX: tapping the specialist avatar opens a fullscreen dark lightbox (large image, close via X / backdrop / Escape) from the local /operators/specialists/ asset. i18n: ~17 new keys + ANR label updates, 12/12 parity with real fa. Tests updated with documented reasons (trust/ownership suites re-pinned: ownership asserts no-yield/no-preview, earn tab asserts the simulator + preserved position + lock flow; page sell entries re-pointed to banner-sell/position-sell on Earn; 6 e2e specs re-pinned incl. sell-flow/order-lifecycle position-card on Earn). Verify: unit 1105/1105 (133 files; yield section + its test deleted, preview file deleted) OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings OK; npm run check exit 0 OK; full e2e 45 passed / 6 skipped (money-path) with 2 known parallel-load flakes (order-lifecycle reload + property-detail secondary gallery - both pass standalone 6/6) OK; Earn tab screenshot reviewed (5-tab strip with Earn selected). Next: user review (STOP).
- **2026-09-14** - **VILLA 1 breathing pass (user-directed wave 4: visual breathing + typography de-weighting + Earn reinforcement ONLY - no new features, no new tabs, no model changes; nothing committed).** (1) TYPOGRAPHY: section titles across all five estate tabs font-semibold -> font-medium (20 files); FactRow values semibold -> medium (hero numbers in the 4-stat grid, net card and Earn metrics keep their weight - hierarchy now: hero numbers strong, row figures medium, labels/captions quiet); headline texts (why-card, location title, earn headline) medium; disclaimers/footers dropped to muted-foreground/80 and /70 so they stop competing with the numbers. (2) BREATHING: FactRow rows py-1 -> py-1.5 everywhere (specs/location/net/legal all breathe); IconPointsGrid gap widened (gap-x-4/gap-y-3 - Why highlights no longer truncate: full wrapping labels; amenities grid airier); scenario honesty note folded from a chrome block into one quiet 0.6875rem line; Owner Stay explainer clamped to 2 lines + title weight down. (3) DEDUP: Growth potential keeps ONE short caption (the long locked paragraph render removed - it repeated the same concept twice); Exit & liquidity dropped the repeated Withdraw sentence (label+value carry it once); position-income block compacted (paid-note moved into the accrued row caption; no dead space for non-owners). (4) EARN: BorderBeam on the Earn TAB CHIP is now PERMANENT while the tab is selected (active = selected; off on switch - the one-shot timer is gone) and the card beam stays mounted only while the tab is visible at strength 0.6 (calmer); card rebuilt to the contract layout - title, one-line subtitle, stepper + investment row, two equal metrics with ONE caption line each (Base scenario / 12 mo - 3-5%), compact inset After-12-months snapshot (income approx + value-of-share range), one tiny disclaimer, Buy CTA; growth label shortened to 'Value growth' (12 mo already in the caption). i18n: caption value updates across 12 locales (parity kept, real fa). Tests: all green with the same testids - no behavioral pins changed beyond copy. Verify: unit 1105/1105 (133 files) OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings OK; npm run check exit 0 OK; e2e estate suites 22/22 OK + full suite 46 passed / 6 skipped OK; screenshots reviewed (breath-overview/specs/income/earn) - the page reads quieter and Earn fits one view. Next: user review (STOP).
- **2026-09-14 (2)** - **VILLA 1 wave 5 (user escalation: 'literally nothing changed' - beam invisible + weights still heavy; screenshots reviewed).** ROOT CAUSES found and fixed: (a) the Earn-chip beam was only active while the tab was SELECTED - from every other tab it was off (invisible), and on the selected BLUE chip the ocean-blue beam was invisible (blue-on-blue); (b) one weight degree (semibold->medium) is imperceptible at 15px on dark. FIXES: (1) CHIP BEAM PERMANENT ALWAYS - active unconditionally, size sm (full compact glow, not the bottom line), strength 0.85, colorVariant OCEAN when unselected and COLORFUL when selected (different color per state, exactly per directive); verified rendering in browser probes (visible glow ring around the Earn chip in both states). (2) CARD BEAM strength 0.6 -> 0.8 (clearly visible on the dark card edge). (3) TYPOGRAPHY one more degree: all estate-page section titles medium -> font-NORMAL (21 files incl. Owner Stay); PropertyHero page title semibold -> medium. (4) RED-MARK leftovers: Why-highlights un-truncated (full wrapping labels, no ellipsis); specialist bio replaced the measurement-based ClampText with a DETERMINISTIC length-based 3-line clamp + Read more/Show less toggle (the bio rendered unclamped in the user's build); Risk disclosures collapsed from 6 separate cards into ONE card with hairline-divided rows; document titles shortened to 2-4 words each (no villa-name suffix, no qualifiers - 'each character less is better'); growth research label shortened to 'Research upper estimate' (no ellipsis); bio toggle uses a property-ns key. Verify: unit 1105/1105 OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings OK; npm run check exit 0 OK; full e2e 47 passed / 6 skipped / 0 failed OK; browser probes confirm the chip glow in BOTH states and the 3-line bio clamp. Nothing committed. Next: user review (STOP).
- **2026-09-14 (3)** - **Earn chip beam upgraded to pulse-inner (user directive: the effect must fill the WHOLE component, not just the border).** PropertyTabs Earn chip: size sm -> pulse-inner (breathing glow filling the entire chip, permanent in both states, colorVariant ocean when unselected / colorful when selected, strength 0.85, theme dark). Verified in browser probes: unselected chip shows the purple-blue breathing fill, selected chip the colorful fill. Verify: unit 1105/1105 OK, tsc clean OK, lint 0 errors + 6 pre-existing warnings OK, e2e property-detail + estate-economics 12/12 OK. Nothing committed. Next: user review (STOP).
- **2026-09-14 (4)** - **FINAL Earn-chip effect (user directive after rejecting the metal look): back to border-beam, animated and visible.** MetalFx uninstalled (chip no longer uses it; nothing imports it). Earn chip = BorderBeam size=md + colorVariant=ocean (the design palette's blue family, azure #229ED9) + strength=1 + theme=dark + active permanently — a traveling animated beam blooming around the chip border, verified in browser probes (two frames 450ms apart show the bloom traveling). The idle earn chip is translucent (bg-transparent) so the beam reads cleanly on the dark strip. Card beam on Earn unchanged (BorderBeam md/ocean/0.8). Verify: unit 1105/1105 OK; tsc clean OK; lint 0 errors + 6 pre-existing warnings OK; e2e property-detail + estate-economics + buy-flow 17/17 OK. Nothing committed. Next: user review (STOP).
- **2026-09-14 (5)** - **Earn-chip beam: half speed, non-uniform, label always readable (user directive; the beam's z-layers were painting over the selected chip's label).** (1) HALF SPEED: the chip beam now pins an explicit duration (base 16s, ~double the package default) via the border-beam duration prop. (2) NON-UNIFORM: a random-duration driver re-picks the spin duration within 10-22s at random 4-7s intervals, so the beam never settles into a uniform rotation. (3) LABEL READABLE WHEN ACTIVE: the beam renders in its own absolute layer BEHIND the label chip (isolate + pointer-events-none), and the selected chip face is translucent blue (bg-primary/80) - the beam glows around/through it while 'Earn' stays crisp; idle chip keeps bg-surface-2/60. Structure: relative container -> absolute beamed spacer layer (aria-hidden) -> label button (relative z-[1]). Verified with browser probes in both states (label crisp, glow animating behind). Verify: unit 1105/1105 OK; tsc clean OK; e2e property-detail + estate-economics + buy-flow 17/17 OK. Nothing committed. Next: user review (STOP).
