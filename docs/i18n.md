# DigiHouse i18n guide

Multi-language support uses **`next-intl`** (client provider, no locale URL prefix — Telegram Mini Apps keep stable deep links).

## Supported locales (12)

| Code | Language | Direction |
|------|----------|-----------|
| `en` | English (default / source of truth) | LTR |
| `fa` | Persian (Farsi) | RTL |
| `ar` | Arabic | RTL |
| `ru` | Russian | LTR |
| `de` | German | LTR |
| `tr` | Turkish | LTR |
| `fr` | French | LTR |
| `es` | Spanish | LTR |
| `pt` | Portuguese | LTR |
| `zh` | Chinese (Simplified) | LTR |
| `hi` | Hindi | LTR |
| `id` | Indonesian | LTR |

## Layout

```
messages/
  en.json   # source of truth
  fa.json ar.json ru.json de.json tr.json
  fr.json es.json pt.json zh.json hi.json id.json
src/i18n/
  config.ts          # LOCALES, RTL_LOCALES, LOCALE_META, localePickerLabel
  detect-locale.ts   # Telegram language_code → AppLocale
  get-messages.ts    # dynamic import of catalogs
src/components/i18n/
  LocaleProvider.tsx # NextIntlClientProvider + dir/lang on <html>
src/components/settings/
  LanguageSelector.tsx  # row + nested picker Sheet
```

## How locale is chosen

1. **Persisted preference** — `useSettingsStore().locale` (`null` = Auto).
2. **Telegram** — `initDataUser.language_code` (SDK signal).
3. **Browser** — `navigator.language` (dev outside TMA).
4. Fallback: **`en`**.

Stored in `localStorage` under `digihouse-settings`.

## Language UI (Settings)

- Row shows **Language** + current label (e.g. `English` or `Auto (Telegram)`) + chevron.
- Tap opens a **Telegram-style bottom sheet** listing Auto + all 12 languages (native name, English in parentheses when different).
- Selection persists immediately and closes the sheet.

## Using translations in UI

```tsx
"use client";
import { useTranslations } from "next-intl";

export function Example() {
  const t = useTranslations("home");
  return <h1>{t("portfolioValue")}</h1>;
}
```

Namespaces: `common`, `tabs`, `header`, `onboarding`, `home`, `marketplace`, `property`, `earnings`, `portfolio`, `settings`.

Interpolation: `t("myPropertiesCount", { count: 3 })` → `{count}` in JSON.

## RTL

- `LocaleProvider` sets `document.documentElement.dir` to `rtl` for `fa` / `ar`.
- Prefer logical Tailwind: `start`/`end`, `ps`/`pe`, `ms`/`me`, `text-start`/`text-end`.
- Mirror chevrons with `rtl:rotate-180`.
- Onboarding carousel flips swipe direction in RTL.

## Adding a new language

1. Add the code to `LOCALES` and `LOCALE_META` in `src/i18n/config.ts`.
2. If RTL, add it to `RTL_LOCALES`.
3. Copy `messages/en.json` → `messages/<code>.json` and translate values (keep keys identical).
4. Register the loader in `src/i18n/get-messages.ts`.
5. Extend `detectLocaleFromCode` for Telegram/browser variants if needed.
6. Run `npm run check` and spot-check Settings → Language (+ RTL if applicable).

## Rules

- **English is the source of truth** — never invent English only in another file.
- Keep keys **namespaced** and stable.
- Do not put locale segments in routes (`/fa/home`) — TMA start URLs stay fixed.
- Seed property titles/locations stay in source language until a CMS exists.
- MVP payout honesty copy must stay accurate in every language (simulated / post-MVP).
