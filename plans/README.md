# Implementation Plans — Phase 3 Integration Audit

Generated by the improve skill on 2026-07-29. HEAD SHA: `f1f5337`.
Execute in the dependency order below. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Focus | Status |
|------|-------|----------|--------|------------|-------|--------|
| 001 | SETTLEMENT_MODE end-to-end (P3-10) | P1 | S | — | honesty badge correctness, ADR-001 §4 compliance | TODO |
| 002 | Indexer characterization tests | P1 | M | 001 | test safety net before indexer changes | TODO |
| 003 | Fix indexer sharesOwned + add audit events | P1 | S | 002 | correctness (onchain landmine) + audit gap | TODO |
| 004 | Buy confirm transactional safety | P1 | M | — | money-path race fix | TODO |
| 005 | Payout idempotency ordering fix | P2 | S | — | crash-scenario stuck distribution | TODO |

Status: TODO | IN PROGRESS | DONE | BLOCKED(reason) | REJECTED(reason)

## Dependency notes

- 002 (indexer tests) must precede 003 (indexer fix) — no changes to untested code.
- 001 (SETTLEMENT_MODE) is independent but foundational for badge correctness; recommended before other plans.
- 004 (buy confirm) and 005 (payout) are independent of each other and of the indexer plans — can run in parallel.

## Findings considered and rejected

- "No Zod validation on endpoints": manual `isPositiveInt` + trim checks adequate for MVP; add Zod on next endpoint change.
- "In-memory rate limiter per-instance": by-design for monolith staging; Redis-backed rate limit is Phase 4.
- "Wallet address exposure in orders": `makerAddress` is user-set wallet; not P3 scope.
- "Duplicate TON SDKs" (`ton` 13.9.0 + `@ton/ton` 16.3.0): `ton` is re-export from `@ton/ton` for compat; not a dupe.
- "Onchain buy prepare messages (P3-04)": deprecated as blocked on P2 contracts — not a code bug.
