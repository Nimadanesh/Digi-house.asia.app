# Implementation Plans — Improve Audit Index

Generated by the improve skill. Two batches exist: the **Phase 4 batch** (2026-07-30, plans 001–009, all done) and the **Batch 2 audit** (2026-08-12, plans 010–019). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status — Batch 2 (2026-08-12, `4c887c9`)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 010  | Gate tests + API/shared typecheck in CI | P1 | S | — | TODO |
| 013  | Remove dead TON deps (`ton`, `@ton/ton`, `@ton/crypto`) | P2 | S | — | TODO |
| 016  | Declare `@tonconnect/ui` + `@tonconnect/sdk` as direct deps | P2 | S | — | TODO |
| 012  | Constant-time admin secret comparison | P2 | S | — | TODO |
| 014  | Derive `totalEarningsUsd` in API portfolio (mock/api parity) | P1 | S | — | TODO |
| 011  | Make buy settlement transactional + holding upsert atomic | P1 | L | 010 | TODO |
| 017  | Bump `drizzle-orm` to ^0.45 (SQL-injection advisory) | P2 | M | 013, 016 | TODO |
| 018  | Bump `next` + `eslint-config-next` to 16.3.0 (advisories) | P2 | M | 013, 016 | TODO |
| 015  | Tests for profile-setup gate + form + `useUpdateProfile` | P2 | M | — | TODO |
| 019  | Playwright E2E web-first + self-contained `webServer` | P2 | M | 018 (soft) | TODO |

## Phase 4 batch (2026-07-30) — all DONE

| Plan | Title | Status |
|------|-------|--------|
| 001  | Prevent CSV formula injection in portfolio export | **DONE** (`9286095`) |
| 002  | Fix admin property creation crash from `draft` status not in DB CHECK constraint | **DONE** (`ea85d7b`) |
| 003  | Add logging and fail-closed behavior to Redis rate limiter | **DONE** (`97d2eb9`) |
| 004  | Fix PropertyDocumentsList empty-state dead code | **DONE** (`d2728f0`) |
| 005  | Fix false "Copied!" feedback when clipboard write fails | **DONE** (`ab27767`) |
| 006  | Extract shared portfolio data-fetch helper to eliminate CSV math duplication | **DONE** (`f0da373`) |
| 007  | Add tests for admin pause/unpause endpoints | **DONE** (`60fb84d`) |
| 009  | Add ownership check to document download URL endpoint | **DONE** (Batch 2; file `009-sec-document-download-ownership-check.md`) |

## Dependency notes

- **010 first**: CI/typecheck gating is the verification baseline. 011 (money refactor) depends on it so the refactor's tests are CI-gated.
- **013 and 016 before 017/018**: both touch `package.json`/`package-lock.json` and must land before the audit-sensitive bumps so `npm audit` deltas are interpretable. Run 013 and 016 **sequentially** (shared lockfile — parallel executors would conflict).
- **018 before 019 (soft)**: E2E should re-verify the post-bump Next; 019's `webServer` makes that one-command.
- **011 risk note**: the transactional seam (drizzle `db.transaction` + tx-bound stores) is the one place the plan may need a human decision if the store factories can't be bound to a `tx`; the executor is instructed to STOP rather than improvise.

## Findings considered and rejected (Batch 2)

- **Money units as nominal types** (branded `MinorUsd`/`NanoTon`): preventive, MED confidence, cross-cutting (touches every money boundary). Not planned; reconsider after the settlement refactor (011) lands.
- **Framer Motion declared-not-installed docs drift** (`TECH_STACK.md`/`DESIGN_SYSTEM`): a docs decision, not a bug. Options tracked for the maintainer; see Direction in the audit report.
- **Node engine `22.x` vs `.nvmrc`/CI `24`**: metadata drift; fold into future dependency housekeeping rather than a standalone plan.
- **Buy intents never garbage-collected** (unbounded `buy_intents` growth): real (HIGH confidence) but lower leverage than the 10 planned items; deferred.
- **`apps/api/README.md` env table stale / "rate limiting" listed Out of Scope**: docs-only, S; deferred until the dep bumps settle so the README is written once.
- **`@telegram-apps/sdk-react` valibot/ReDoS advisory**: the "fix" is a breaking downgrade to 2.x conflicting with the 3.x integration; explicitly **not** auto-applied — tracked in plan 018's maintenance note.

## Phase 4 findings considered and rejected (from the prior audit)

- SEC-03 (publicUrl bypasses signed URL TTL) — Batch 2 (superseded by 009)
- SEC-06 (doc download lacks shareholding check) — Batch 2 (superseded by 009)
- ARCH-01 / COR-04 (CSV math duplication) — covered by plan 006
- TST-02 (Redis rate limiter test coverage) — covered by plan 003
- All other prior findings (ARCH-02…05, COR-05…08, SEC-04…05, TST-03…06) — out of scope for that session; not re-audited since.
