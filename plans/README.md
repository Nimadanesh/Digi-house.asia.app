# Implementation Plans — Phase 4 Improve

Generated by the Phase 4 audit on 2026-07-30. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Prevent CSV formula injection in portfolio export | P1 | S | — | **DONE** (`9286095`) |
| 002  | Fix admin property creation crash from `draft` status not in DB CHECK constraint | P1 | S | — | **DONE** (`ea85d7b`) |
| 003  | Add logging and fail-closed behavior to Redis rate limiter | P1 | S | — | **DONE** (`97d2eb9`) |
| 004  | Fix PropertyDocumentsList empty-state dead code | P2 | S | — | **DONE** (`d2728f0`) |
| 005  | Fix false "Copied!" feedback when clipboard write fails | P2 | S | — | **DONE** (`ab27767`) |
| 006  | Extract shared portfolio data-fetch helper to eliminate CSV math duplication | P2 | S | — | **DONE** (`f0da373`) |
| 007  | Add tests for admin pause/unpause endpoints | P2 | S | — | **DONE** (`60fb84d`) |

## Dependency notes

- 002 is logically independent but the admin test in 007 creates draft properties — if the schema is not updated first (002), the DB constraint fix must be in place before running 007 against a real DB. The in-memory store used by tests is unaffected, so 007 does not technically depend on 002 for test-passing.
- 006 changes the same file as 001 (`portfolio.ts`). Apply 001 first (it's a one-line change in `csvEscape`), then 006 refactors the whole file. The drift check in 006's plan accounts for this — the executor checks `git diff` and reports if the file changed.

## Findings considered and rejected

All Phase 4 findings not in Batch 1 or Batch 2 are currently out of scope for this improve session:
- SEC-03 (publicUrl bypasses signed URL TTL) — Batch 2
- SEC-06 (doc download lacks shareholding check) — Batch 2
- ARCH-01 (CSV route duplicates portfolio query + math) — covered by plan 006
- TST-02 (Redis rate limiter test coverage) — covered by plan 003
- COR-04 (CSV math duplication) — covered by plan 006
- All other findings (ARCH-02 through ARCH-05, COR-05 through COR-08, SEC-04 through SEC-05, TST-03 through TST-06) — out of scope for this session
