"use client";
// File responsibility: legacy Home navbar path — renders the shared tab header
// (AppShell owns it on all tab routes; this alias keeps the old import path working).
export { AppHeader as HomeNavbar } from "@/components/layout/AppHeader";
